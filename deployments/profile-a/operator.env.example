# Profile-A operator template — CBP-WP-020 (D-055).
# Copy this file into the local operator workspace OUTSIDE this repository,
# fill every required value there, and never copy a filled file back.
# Empty required values are intentional and fail-closed.
CBP_CONTROL_PLANE_IMAGE=
CBP_DATA_WORKER_IMAGE=
CBP_CONTROL_PLANE_UID=
CBP_CONTROL_PLANE_GID=
CBP_DATA_WORKER_UID=
CBP_DATA_WORKER_GID=
CBP_DEPLOYMENT_PROFILE=A
