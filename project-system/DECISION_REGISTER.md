# Decision Register – Core Brain Pilot

| Feld | Wert |
| --- | --- |
| Phase | **Phase 0 COMPLETE** · Phase 1 AUTHORIZED FOR PLANNING |
| Überarbeitet in | **CBP-WP-011** |
| Autoritätsklasse | A2 |
| Stand | 2026-07-21 |

> **Ablageabweichung.** NDF v1.0.0 sieht `project-brain/DECISIONS.md` vor.
> Es existiert bewusst nur **eine** von beiden — AB-04.

## Getroffene Entscheidungen

### Aus CBP-WP-001 und CBP-WP-002

| ID | Entscheidung | Klasse | Datum | ADR |
| --- | --- | --- | --- | --- |
| D-001 | Verbindlich nach NDF v1.0.0; keine v1.1-Planung | A0 | 2026-07-20 | offen |
| D-002 | Phase 0 ist Discovery und Scope Lock; keine produktive Implementierung | A0 | 2026-07-20 | offen |
| D-003 | Commit-, Push- und Release-Autorität ausschließlich beim Human Maintainer | A0 | 2026-07-20 | offen |
| D-004 | Markdown ist das kanonische Wissensformat | A2 | 2026-07-20 | offen |
| D-005 | Abgeleitete Daten werden nicht versioniert und sind nie autoritativ | A2 | 2026-07-20 | offen |
| D-006 | Fünf Datenklassen; Secrets nie in Repository, Index, Context Pack oder Modellkontext | A2 | 2026-07-20 | offen |
| D-007 | Keine `LICENSE`-Datei in Phase 0 | A0 | 2026-07-20 | — |
| D-008 | Register in `project-system/`; keine Doppelstruktur | A2 | 2026-07-20 | offen |
| D-009 | NDF Prompt Modes sind Full, Standard, Short. „Lean" ist ausschließlich der Name von B1 | A0 | 2026-07-20 | offen |
| D-010 | A5-Projektübergabe dauerhaft als kanonische Quelle im Repository | A0 | 2026-07-20 | — |
| D-011 | Quellenklassifikation: PDF A4, Textfassung A6, Übergabe A5 | A0 | 2026-07-20 | — |
| D-012 | UTF-8 mit echten deutschen Umlauten | A0 | 2026-07-20 | — |
| D-013 | Dedizierte Linux-VM ist Referenzbetrieb; Docker Compose vorgesehene Laufzeit darin | A0 | 2026-07-20 | offen |
| D-014 | Wiki, Graph und Web-UI nicht vor bestandenem Retrieval-Pilot-Gate | A0 | 2026-07-20 | offen |

### Neu aus CBP-WP-003 — Human Discovery Intake

Alle folgenden Entscheidungen wurden vom Human Maintainer **ausdrücklich**
getroffen. Quelle jeweils
[../docs/discovery/HUMAN_DISCOVERY_INPUT.md](../docs/discovery/HUMAN_DISCOVERY_INPUT.md).

| ID | Entscheidung | Status | Autorität | Quelle | Datum | Betroffene Kriterien | Konsequenz |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **D-015** | Erstes Pilotprofil ist eine Proxmox-VM mit dedizierter Linux-VM als Referenzbetrieb | `accepted` | **A0** | HDI A1 | 2026-07-20 | F-1 | Referenzprofil A ist gewählt; Profile B–E bleiben dokumentierbar |
| **D-016** | Docker Compose ist die **bevorzugte** Anwendungslaufzeit innerhalb dieser VM | `accepted` | **A0** | HDI A1 | 2026-07-20 | F-2 | Präzisiert D-013 und **hebt die Abschwächung Ü-02 aus CBP-WP-002 auf** — siehe Hinweis unten |
| **D-017** | Deployment-Neutralität wird beibehalten: allgemeine Linux-VM, Docker/OCI und Einzelplatz bleiben dokumentierbare Profile | `accepted` | **A0** | HDI A1 | 2026-07-20 | F-5 | Kein Proxmox-Lock-in; Capability 29 bestätigt |
| **D-018** | Multi-User und Multi-Tenant sind kein Pflichtumfang des ersten Piloten; spätere Teamnutzung darf nicht verhindert werden | `accepted` | **A0** | HDI A2 | 2026-07-20 | A-1, A-2 | Single-User als Einstieg; Architektur bleibt erweiterbar |
| **D-019** | PDF- und Office-Dokumente gelangen nicht ungeprüft in den kanonischen Bestand; kontrollierte Ingest- und Quarantäne-Pipeline erforderlich | `accepted` | **A0** | HDI A3 | 2026-07-20 | D-1, D-3 | Capability 5 (Ingest-Quarantäne) wird Voraussetzung für Nicht-Markdown-Quellen |
| **D-020** | `confidential` ist nicht Teil des ersten Piloten, muss aber architektonisch unterstützt werden | `accepted` | **A0** | HDI A4 | 2026-07-20 | D-7 | D-7 wird `not-applicable` für den Pilot; Klasse bleibt im Modell |
| **D-021** | `excluded-from-ai` ist von Anfang an im Daten- und Berechtigungsmodell zu führen; Sperrwirkung mit synthetischen oder unkritischen Testdaten zu prüfen | `accepted` | **A0** | HDI A4 | 2026-07-20 | D-5 | Macht R-30 zu einer konkret prüfbaren Anforderung; **strenger als das bisherige Fundament** |
| **D-022** | Personenbezogene Daten sind nicht Teil des ersten Piloten; spätere Aufnahme erfordert vorher eine gesonderte Datenschutz- und Rechtsgrundlagenprüfung | `accepted` | **A0** | HDI A4 | 2026-07-20 | D-6 | D-6 wird `not-applicable`; OD-09 wird vertagt statt geschlossen |
| **D-023** | Zugriff nur über privates VPN oder privates Netz; keine öffentliche Freigabe interner Dienste | `accepted` | **A0** | HDI A5 | 2026-07-20 | C-5 (Grundsatz) | Zugriffsprofil festgelegt; Technologiewahl bleibt Deployment Required |
| **D-024** | Web-UI und mobile Nutzung gehören zum Pilotumfang; die Web-UI aber erst nach funktionierendem Index, Suche, Brain-First-Retrieval und Benchmark | `accepted` | **A0** | HDI A6 | 2026-07-20 | A-4, A-5, F-6 | Präzisiert D-014 mit einer konkreten Vorbedingung |
| **D-025** | Native Obsidian-Nutzung, Wiki-Pilot und externe Connectoren werden vertagt; Knowledge Graph ist nicht Pilotumfang | `accepted` | **A0** | HDI A6 | 2026-07-20 | A-7 | Vier Conditional-Kriterien bleiben inaktiv |
| **D-026** | Dreistufiges Kriterienmodell: Core Required, Deployment Required, Conditional. G0 sperrt den allgemeinen Produkt-Scope, nicht die Installationsdetails | `accepted` | **A0** | Nova-Review CBP-WP-003 | 2026-07-20 | alle 47 | Blocker von 45 auf 25 reduziert; separates Deployment-Readiness-Gate später zu definieren |
| **D-027** | **G0 – Discovery and Scope Lock: APPROVE G0 WITH NOTES.** Die Freigabe autorisiert ausschließlich die Planung von Phase 1; keine Freigabe für produktiven Betrieb, produktiven Ingest, öffentliche Erreichbarkeit oder zusätzliche sensible Datenklassen. Fünf Nachweise vor produktivem Betrieb; Web-UI und mobile Nutzung erst nach gemessenem Retrieval-Piloten | `accepted` | **A0** | Entscheidungsblock in G0_SCOPE_LOCK_REVIEW | 2026-07-21 | alle 25 Core-Kriterien | Phase 0 COMPLETE; Phase 1 AUTHORIZED FOR PLANNING |
| **D-028** | **ADR-0006 angenommen:** produktive und private Wissensbestände bleiben außerhalb des allgemeinen Core-Repositorys und werden über logische Source Slots und deploymentspezifische, fail-closed Mappings angebunden | `accepted` | **A0** | Entscheidungsblock in G0_SCOPE_LOCK_REVIEW | 2026-07-21 | D-1 | ADR-0006 von `proposed` auf `accepted` (A1); OD-05, OD-06, OD-26 bleiben offen |

### Neu aus CBP-WP-009 — Repository Boundary Decision

Beide Entscheidungen am **2026-07-21**, Autorität **A0**, Quelle: direkte
Human-Maintainer-Entscheidung im Entscheidungsblock. Wortlaut unverändert in
[ADR-0007](../docs/decisions/ADR-0007-repository-und-workspace-grenze.md).

| ID | Entscheidung | Status | Autorität | Quelle | Datum | Betroffen | Konsequenz |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **D-029** | **Teil A — Zielstruktur des Core-Repositorys: Ziel-Monorepo nach Layout-Option B.** Bereiche `core/`, `adapters/`, `deployments/`, `config/`, `docs/`, `examples/`, `tests/`. **Autorisiert keine Verschiebung**; das aktuelle Layout bleibt bis zu einem separaten, ausdrücklich freigegebenen Migrations-Work-Package. Migration nachvollziehbar, schrittweise, rücksetzbar und **ohne Verlust der Git-Historie** | `accepted` | **A0** | Entscheidungsblock CBP-WP-009 | 2026-07-21 | **OD-26 Teil A** | **ADR-0007** (A1); AB-03…AB-08 bleiben offen (OD-29) |
| **D-030** | **Teil B — Bereichsmodell W-3: privater Operator-Workspace außerhalb des Core-Repositorys.** Private und produktive Wissensbestände, konkrete Source Mappings, private Collection-Konfigurationen und **operatorbezogene kanonische Registry-Metadaten** liegen außerhalb. Runtime-Daten bilden einen dritten, separaten Bereich. **Secrets nirgends im Klartext** — nur Verweise auf einen getrennten Secret Store. Eine Runtime-Kopie der Source Registry ist **nie** die einzige Quelle kanonischer Registry-Metadaten | `accepted` | **A0** | Entscheidungsblock CBP-WP-009 | 2026-07-21 | **OD-26 Teil B** | **ADR-0007** (A1); konkretisiert ADR-0006; Überführung nach W-2 bleibt vorbereitet, **nicht beschlossen** |

> **Beide Entscheidungen legen Zielarchitektur fest, keine Umsetzung.** Es
> wurde keine Datei verschoben, kein Verzeichnis angelegt und kein Workspace
> erzeugt.

### Neu aus CBP-WP-010 — Pilot Source Mapping Specification

Alle drei am **2026-07-21**, Autorität **A0**, Quelle: direkte
Human-Maintainer-Entscheidung im Entscheidungsblock. Wortlaut unverändert in
[ADR-0008](../docs/decisions/ADR-0008-pilot-source-mapping-konvention.md).

| ID | Entscheidung | Status | Autorität | Quelle | Datum | Betroffen | Konsequenz |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **D-031** | **Teil A — Kanonisches Mappingformat: YAML 1.2 Strict Subset mit JSON-Schema-Validierung.** Nur ein klar begrenzter YAML-Teilumfang; **JSON Schema ist die verbindliche maschinenprüfbare Vertragsgrenze**. Unzulässig sind insbesondere Anchors, Aliases, Merge Keys, benutzerdefinierte Tags, doppelte Schlüssel und mehrere Dokumente je Datei | `accepted` | **A0** | Entscheidungsblock CBP-WP-010 | 2026-07-21 | PS-02, PS-03, PS-04 | **ADR-0008** (A1); Formatregeln F1–F7 im Mapping-Schema |
| **D-032** | **Teil B — Collection-Strategie: hybrid.** Collections primär nach fachlichem Projekt oder fachlicher Domäne; der **Source Slot bleibt verpflichtendes Metadatum** für Provenienz, Ingest-Regeln, Berechtigungen, Löschung und Audit. **Die Collection allein verleiht keine Autoritätsklasse, keine Datenklasse und keine AI-Transfer-Freigabe** | `accepted` | **A0** | Entscheidungsblock CBP-WP-010 | 2026-07-21 | Retrieval, Berechtigungen | **ADR-0008** (A1); Grundsatz M-B |
| **D-033** | **Teil C — Mapping-Granularität: genau eine Source Boundary je Mapping.** Bei PS-02 ein Markdown Root, bei PS-03 ein Git Repository, bei PS-04 ein Handoff Root. **Mehrere Quellen dürfen nicht durch ein gemeinsames Mapping gekoppelt werden** | `accepted` | **A0** | Entscheidungsblock CBP-WP-010 | 2026-07-21 | Rechte, Revisionen, Löschung, Tombstones | **ADR-0008** (A1); Grundsatz M-C |

> **Die drei Entscheidungen legen eine Konvention fest, kein Mapping.** Es
> wurde keine Quelle angebunden, kein Mapping erstellt, nichts aktiviert. Das
> [Aktivierungsgate](../docs/operations/PILOT_MAPPING_ACTIVATION_GATE.md) steht
> auf **`NOT EVALUATED`**.

### Neu aus CBP-WP-011 — Technical Security Foundation Specification

Alle vier am **2026-07-21**, Autorität **A0**, Quelle: direkte
Human-Maintainer-Entscheidung im Entscheidungsblock. Wortlaut unverändert in
[ADR-0009](../docs/decisions/ADR-0009-technische-sicherheitsgrundlage.md).

| ID | Entscheidung | Status | Autorität | Quelle | Datum | Betroffen | Konsequenz |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **D-034** | **Teil A — Service-Identity-Modell: getrennte logische Identitäten für Control Plane und Data Worker** mit minimalen, voneinander unabhängigen Rechten. Die Control Plane darf Canonical Sources nicht verändern und nicht automatisch publizieren; der **Data Worker erhält keine Approval-, Administrations- oder Publish-Rechte**. Konkrete Unix-Benutzer, Container-Identitäten, UID- und GID-Werte erst deploymentspezifisch | `accepted` | **A0** | Entscheidungsblock CBP-WP-011 | 2026-07-21 | KB-01, KB-02, KB-03, KB-05 | **ADR-0009** (A1); Grundsatz S-A |
| **D-035** | **Teil B — Secret-Modell: versionierter, providerneutraler Secret-Reference- und Resolver-Vertrag** plus **OS-geschützter Datei-Provider** für den Pilot, außerhalb von Core-Repository, Operator-Workspace und Runtime. Werte nur read-only an die berechtigte Identität; **niemals in Git, Konfiguration, Umgebungsvariablen, Kommandozeilen, Logs, RT-2, Context Packs, Fehlermeldungen oder Reports**. Referenzen enthalten keine Werte und keine Hostpfade; später ohne Änderung der Mappingkonvention migrierbar | `accepted` | **A0** | Entscheidungsblock CBP-WP-011 | 2026-07-21 | KB-08; **schließt OD-34** | **ADR-0009** (A1); Grundsatz S-B |
| **D-036** | **Teil C — Netzwerk-Egress: deny-by-default mit expliziter Allowlist**, gebunden an **Ziel, Provider, Zweck und Service-Identität**. Vor jeder externen Übertragung zusätzlich Datenklasse, AI-Transfer-Policy, Approval-Zustand, Zweck und freigegebenes Ziel prüfen. **Lokale Suche und Retrieval funktionieren ohne externen Netzwerkzugriff**; `excluded-from-ai` bleibt unabhängig von jeder Netzfreigabe blockiert | `accepted` | **A0** | Entscheidungsblock CBP-WP-011 | 2026-07-21 | KB-10, KB-11 | **ADR-0009** (A1); Grundsatz S-C |
| **D-037** | **Teil D — Operational Evidence: logisch append-only, integritätsverkettet, aufbewahrungs- und sicherungspflichtig.** Korrekturen durch nachvollziehbare Folgeereignisse statt stillschweigendem Überschreiben; stabile Ereignisidentitäten, getrennte Zugriffsrechte, Backup, Restore-Nachweis, **sichtbare Erkennung von Ketten- oder Integritätsbrüchen**. RT-2 ist weder Cache noch kanonische Wissensbasis und nicht zuverlässig rekonstruierbar. **Konkrete Aufbewahrungsdauer, Speichertechnologie und Implementierung bleiben deploymentspezifisch** | `accepted` | **A0** | Entscheidungsblock CBP-WP-011 | 2026-07-21 | KB-09, KB-12; **schließt OD-35** | **ADR-0009** (A1); Grundsatz S-D |
| **D-038** | **Teil A — Intake-Granularität: genau eine ausdrücklich angegebene Markdown-Datei je Intake.** Keine Verzeichnisrekursion, keine Mehrfachdateien, keine Archive, keine Symlink-Folgen, keine implizite Suche, kein Zugriff auf Canonical-Source-Roots | `accepted` | **A0** | Entscheidungsblock CBP-WP-013 | 2026-07-22 | Quarantäne-MVP, R-32 | **ADR-0010** (A1) |
| **D-039** | **Teil B — Quarantänespeicher: lokaler content-addressed Dateispeicher** mit unveränderlichem Payload und atomarem JSON-Manifest. Root explizit und außerhalb des Core-Repositorys; im Work Package nur temporär mit synthetischen Daten. Objektpfade nur aus validiertem SHA-256; Idempotenz; Kollision blockiert. **Weder Canonical Source noch RT-2, keine produktive Sicherheitsgrenze** | `accepted` | **A0** | Entscheidungsblock CBP-WP-013 | 2026-07-22 | Quarantäne-MVP | **ADR-0010** (A1) |
| **D-040** | **Teil C — Baseline-Scanner: strukturelle Prüfungen plus deterministische Credential- und PII-Indikatoren.** Fest verdrahtete Regeln, keine frei konfigurierbaren regulären Ausdrücke. **Indikatoren, keine vollständige Secret-, PII- oder Klassifikationskontrolle.** Befunde ohne Snippet, Pfad oder Wert | `accepted` | **A0** | Entscheidungsblock CBP-WP-013 | 2026-07-22 | R-01, R-32 | **ADR-0010** (A1) |
| **D-041** | **Teil D — Freigabemodell: drei Zustände `READY_FOR_HUMAN_REVIEW`, `REVIEW_REQUIRED`, `BLOCKED`; keine automatische Promotion.** Kein Zustand bedeutet `approved`, `released`, `enabled` oder `indexed`. `quarantine release` verweigert deterministisch fail-closed | `accepted` | **A0** | Entscheidungsblock CBP-WP-013 | 2026-07-22 | Quarantäne-MVP | **ADR-0010** (A1) |
| **D-042** | **Teil A — Registry-Identität: stabiler Namespace plus Source Key, daraus deterministisch abgeleitete Source ID** (`src-` + 24 Hex des SHA-256 aus Identitätsschema, Namespace, Source Key). Kein Display Name, Pfad, URL oder Inhalt in der ID; abweichende Identität/Definition unter bestehender ID blockiert | `accepted` | **A0** | Entscheidungsblock CBP-WP-014 | 2026-07-22 | Registry-MVP | **ADR-0011** (A1) |
| **D-043** | **Teil B — Registry-Speicher: unveränderliche JSON-Records plus atomar abgeleiteter Katalog außerhalb des Repositorys.** Kanonisches UTF-8-JSON, atomar, keine stille Überschreibung, Idempotenz, Konflikt bei abweichender Definition, kein Teilkatalog bei Integritätsfehler, vollständig rekonstruierbar. **Weder Canonical Source noch RT-2, keine produktive Isolationsgrenze** | `accepted` | **A0** | Entscheidungsblock CBP-WP-014 | 2026-07-22 | Registry-MVP | **ADR-0011** (A1) |
| **D-044** | **Teil C — Lifecycle: `REGISTERED_DISABLED` und `RETIRED`; Retirement als append-only Event; keine Aktivierung.** Keine Löschung, keine Aktualisierung, keine Reaktivierung; idempotent; keine Freitexte, Pfade oder Inhalte im Event | `accepted` | **A0** | Entscheidungsblock CBP-WP-014 | 2026-07-22 | Registry-MVP | **ADR-0011** (A1) |
| **D-045** | **Teil D — Katalogumfang: ausschließlich minimierte Metadaten** (10 Felder je Eintrag); keine Pfade, URLs, Source-Inhalte, Definition Hashes, Owner-Freitexte oder Mapping-Locators. Deterministisch nach `source_id` sortiert, aus Records und Events abgeleitet | `accepted` | **A0** | Entscheidungsblock CBP-WP-014 | 2026-07-22 | Registry-MVP | **ADR-0011** (A1) |
| **D-046** | **Teil A — Dokumentprofil: kanonisches JSON als JSON-kompatibles MVP-Profil des strikten YAML-Teilumfangs**; alle 31 Felddefinitionen unverändert (29 Pflicht + 2 optional). Keine allgemeine YAML-Unterstützung, keine externe Abhängigkeit; doppelte Schlüssel, `NaN`, `Infinity`, BOM, ungültiges UTF-8 und unbekannte Felder blockieren; optionale Felder bleiben optional. Präzisiert **D-031**, ändert ihn nicht | `accepted` | **A0** | Entscheidungsblock CBP-WP-015 | 2026-07-27 | Mapping-Draft-Validator | **ADR-0012** (A1) |
| **D-047** | **Teil B — externe read-only Registry-Bindung über `--source-id` und `--registry`; `source_id` ist kein Mapping-Feld.** Registry bytegenau unverändert; nur `collection`↔`collection_key` und `data_class`↔`data_class` exakt; **verbotene Crosswalks** `project`↔`domain_key`/`namespace`, `ai_transfer_policy`↔`ai_eligibility`, `location_reference`/`operator_reference`↔`source_id` | `accepted` | **A0** | Entscheidungsblock CBP-WP-015 | 2026-07-27 | Mapping-Draft-Validator | **ADR-0012** (A1) |
| **D-048** | **Teil C — genau eine deaktivierte synthetische Boundary mit bestehenden kanonischen Feldern/Werten.** Kein neues `slot:synthetic:`-Präfix, keine neuen Enum-Werte; `slot_id` ∈ {PS-02, PS-03, PS-04}; `location_reference` = belegter synthetischer V7-Platzhalter `synthetic-placeholder-*`; Subpath-Listen leer, `follow_symlinks=false`, `enabled=false`, `read_only=true`. Präzisiert **D-033**, ändert ihn nicht | `accepted` | **A0** | Entscheidungsblock CBP-WP-015 | 2026-07-27 | Mapping-Draft-Validator | **ADR-0012** (A1) |
| **D-049** | **Teil D — ausschließlich nicht persistierter, deterministischer Validierungsreport; Aktivierung immer verweigert.** `mapping_id` wird nach Vertrag (V4/V21) **validiert, nicht berechnet**; keine `map-`+SHA-256-Regel; `draft_sha256`/`policy_sha256` deterministisch; kein gespeicherter Report, keine Registry-Änderung; `VALID_DRAFT` bedeutet keine Freigabe und keine Aktivierung | `accepted` | **A0** | Entscheidungsblock CBP-WP-015 | 2026-07-27 | Mapping-Draft-Validator | **ADR-0012** (A1) |
| **D-050** | **Konsolidierte Freigabe des Mapping-Activation-Gate-Evaluators (WP-016): APPROVE IMPLEMENTATION WITH NOTES; A1** synthetisches Eingabe-/Evidenzmodell; **B1 – eng** (Ausgabestatus nur `NOT_EVALUATED`/`BLOCKED`; `READY FOR ACTIVATION DECISION`/`APPROVED FOR ACTIVATION`/`REVOKED` nicht emittierbar); **C1** fehlende/veraltete/NOT-EVALUATED/widersprüchliche Abhängigkeiten blockieren fail-closed, Human-only nie automatisch erfüllt; **D1** deterministischer, minimierter, nicht persistierter A6-Report. 20 Kriterien fest; Security Foundation/DRC keine Kriterien 21/22; keine Aktivierung, keine Persistenz | `accepted` | **A0** | Entscheidungsblock CBP-WP-016 | 2026-07-27 | Gate-Evaluator | **— (kein ADR)** |
| **D-051** | **Konsolidierte Freigabe des Synthetic Evidence Contract & Provenance Foundation (WP-017): APPROVE IMPLEMENTATION WITH NOTES. A2** Evidence-Bundle mit eingebetteten strukturierten Artefakten (eine synthetische JSON-Datei, kein neuer CLI-Befehl); **B1** negative-evidence integration only (formal gültige, aktuelle synthetische Evidenz erfüllt **nie** zusätzlich `SATISFIED`); **C2** Evidence Schema 2.0, Schema 1.0 fail-closed; **D1** ADR_NOT_REQUIRED unter Scope Lock (reine Konkretisierung ADR-0007/0009, RT-1, keine Persistenz/Promotion/Konfliktpriorität/cross-component Provenance-Autorität); **E2** minimal erweiterter A6-Report (`evidence_contract_revision`/`_sha256` + vier Artefaktzähler). Keine Aktivierung, keine operative/reale Evidenz, kein RT-2 | `accepted` | **A0** | Entscheidungsblock CBP-WP-017 | 2026-07-28 | Evidence-Contract | **— (kein ADR)** |
| **D-052** | **APPROVE CBP-WP-018 GOVERNANCE FOUNDATION WITH NOTES (Phase B0, rein dokumentarisch).** **A1** neue Producer-Klasse `security-control-form` mit Pflichtfeld `control_id`, Runtime-Identität `(criterion, control_id)`; **B1** Evidence Schema **3.0** / Contract-Revision 3.0, Schema 1.0 **und** 2.0 fail-closed; **C1** Security Contract vollständig im Security-Control-Artefakt-Binding (`control_id`, `security_contract_revision`, `security_contract_sha256`); **D1** Scope **S3** (12 dokumentierte, 7 runtime-scoped Controls, 11 `(criterion, control_id)`-Bindungen); **E1** **ADR-0013 `accepted`**. **Partielle Ablösung von D-051:** weiterhin gültig A2/B1/E2; abgelöst C2 (Schema 2.0) und D1 (ADR_NOT_REQUIRED); neu: Schema 3.0, ADR erforderlich und angenommen. **Autorisiert keine technische Implementation, keine Migration, keinen Runtime-Code** — implementierter Stand bleibt Schema 2.0; Kriterium 5 Human-only, Kriterium 9 non-security-structural; negative-evidence-only; keine Security-Freigabe/Enforcement/Aktivierung/RT-2/Persistenz | `accepted` | **A0** | Entscheidungsblock CBP-WP-018 | 2026-07-28 | Security-Readiness-Governance | **ADR-0013** (A1) |
| **D-053** | **APPROVE CBP-WP-018 TECHNICAL IMPLEMENTATION WITH NOTES (Phase B1).** **A1** ADR-0013 technisch umgesetzt: Evidence Schema 3.0, `security-control-form`, explizites `control_id`, Security-Contract-Bindung; **B1** einzige akzeptierte Evidence-Schema-Version **3.0** (Contract-Revision 3.0), Schema 1.0 **und** 2.0 fail-closed, **Gate-Vertrag unverändert** (Revision und Hash); **C1** separater statischer Security Contract (`core/core_brain/gate/security_contract.py`, Revision 1.0) mit **12** dokumentierten Controls, **7** runtime-scoped Controls und **11** runtime-scoped `(criterion, control_id)`-Bindungen; **D1** Integrationssemantik **negative-evidence-only** — kein `SATISFIED` durch Security-Control-Formartefakte, kein Entfernen von `DEPENDENCY_BLOCKED`, keine Human-Decision-Simulation, keine Security-Freigabe; **E1** Report trägt Contract-Revision und -Hash sowie eindeutige Control- und Binding-Zähler, **keine** öffentliche Readiness-Aggregation. Kriterium 5 bleibt Human-only, Kriterium 9 bleibt non-security-structural (keine KB-Bindung). Synthetic-only, read-only, nicht persistent, fail-closed; **keine** reale Security-Evaluation, kein Enforcement, kein RT-2, keine Persistenz, keine Aktivierung | `accepted` | **A0** | Entscheidungsblock CBP-WP-018 | 2026-07-29 | Security-Readiness-Runtime | **ADR-0013** (A1) |
| **D-054** | **APPROVE CBP-WP-019 DEPLOYMENT READINESS INTAKE AND PROFILE-A TARGET SPECIFICATION WITH NOTES.** **A** Profil A wird als **erstes verbindliches Referenzdeployment** bestätigt: Plattform Proxmox VE, Topologie Einzelhost, Gast Ubuntu Server 26.04 LTS amd64, Laufzeit Docker Compose **innerhalb einer dedizierten VM**; **B** Ressourcen 4 vCPU, 8 GiB RAM, 64 GiB Systemdisk, 250 GiB **getrennte** Datendisk, NVMe-basierter LVM-Thin-Storage — **Pilotwerte, keine Produktmindestanforderungen**; **C** Zugriff ausschließlich über privates LAN oder WireGuard, **keine öffentliche direkte Freigabe**, WireGuard bleibt **austauschbare deploymentspezifische** Infrastrukturentscheidung, Egress bleibt **deny-by-default**; **D** Backupmodell **zweistufig**: vollständiges **Proxmox-VM-Backup wöchentlich** auf **physisch separate NAS** **plus** **tägliche Sicherung der kanonischen CBP-Daten**; vollständig reproduzierbare abgeleitete Daten benötigen **kein** eigenes Backup (Rebuild aus kanonisch + Registry); **RPO 24 Stunden** (getragen von der **täglichen kanonischen Sicherung**, nicht vom wöchentlichen VM-Backup), **RTO 8 Stunden**; Backuptechnik und -ziel bleiben deploymentspezifisch, die konkrete technische Umsetzung der täglichen kanonischen Sicherung ist **noch nicht ausgeführt**; der Human Maintainer akzeptiert seine bisherigen erfolgreichen Proxmox-Restore-Erfahrungen als Nachweis für **DRC-16 im Umfang des Betreiber-Backup-Regimes**; **E** DRC-Lifecycle bleibt **L0** — ein einstufiger DRC vor der ersten kontrollierten Bereitstellung; **DRC-16 wird präzisiert** auf den durchgeführten Restore-Test der Betreiber-Backupkette, **nicht** CBP-Daten und **nicht** RT-2; der spätere CBP-/RT-2-Restore bleibt im **Security Foundation Readiness Gate (Punkt 19)**; **F** der DRC wird von **18 auf 19 Prüfpunkte** erweitert um **DRC-19 – RT-2-Aufbewahrung**; DRC-19 besitzt **keine unmittelbare G0-Herkunft**, der historische G0-Nachweis bleibt unverändert; **G** OD-34 bleibt durch D-035 geschlossen, der OS-geschützte Datei-Provider bleibt Pilotmodell, konkreter Providertyp (**`file`**), abstrakte Schutzklasse, berechtigte Service-Identitätsklassen (**`svc-control-plane`**, **`svc-data-worker`**), Rotations- und Widerrufsverfahren sowie Betreiberrolle sind **bestätigt** — **DRC-17 `ready`**; **OD-20 bleibt außerhalb des DRC** und einem späteren Capability-Paket zugeordnet; **H** **ADR_NOT_REQUIRED** — keine neue Trust Boundary, kein neuer Gate-Typ, keine neue Produktabhängigkeit, WireGuard und Proxmox-Backup bleiben Deploymentwahl, Secret-Providertyp aus D-035, Profil A bereits entschieden, DRC-19 erweitert nur einen bestehenden Prüfvertrag. Ergänzend zu **F** ist die **RT-2-Aufbewahrung** bestätigt: **Mindestaufbewahrung 365 Tage** für **alle** `retention_class`-Werte einschließlich Security- und Incident-Evidenz, **danach dauerhafte Aufbewahrung**, **keine automatische Löschung**, **kein separates Archiv**, Owner **Human Maintainer / Betreiber**. Alle **19** DRC-Einzelkriterien stehen auf `ready`. **Human DRC Decision Gate: APPROVED** — der Human Maintainer hat am **2026-07-29** für **Profil A** den **DRC-Gesamtstatus** auf **`APPROVED BY HUMAN MAINTAINER`** gesetzt (Ausführung des in dieser Entscheidung vorgesehenen Human Gates, **keine zusätzliche Decision, kein ADR**). Der Gesamtstatus wurde **nicht** automatisch aus den Einzelwerten abgeleitet. **Genehmigter Umfang:** die für Profil A erforderlichen Deployment-Readiness-Werte sind dokumentiert und akzeptiert; eine erste kontrollierte Bereitstellung darf in einem **späteren, ausdrücklich autorisierten** Work Package **vorbereitet** werden. **Nicht genehmigt:** Installation, Bereitstellung, Betriebs- oder Produktivfreigabe, Security-Readiness-Freigabe, Bestätigung implementierter/getesteter/erzwungener Sicherheitskontrollen, Mapping-Aktivierung, Gate-Ausführung oder -Freigabe, Capability-Freigabe, Source-Aktivierung, RT-2-Betriebsfreigabe sowie Backup-/Restore-Evidenz für CBP oder RT-2. **R-20 bleibt offen**; zwölf KB-Kontrollen bleiben `DOCUMENTED ONLY`; beide Runtime-Gates bleiben `NOT EVALUATED`; Capabilities **0 von 29** | `accepted` | **A0** | Entscheidungsblock CBP-WP-019 | 2026-07-29 | Deployment Readiness Profil A | — (**ADR_NOT_REQUIRED**) |
| **D-055** | **APPROVE ADDITIVE PROFILE-A DEPLOYMENT ARTIFACT ROOT AND CBP-WP-020 SCOPE.** **A — Additive Struktur:** Für Phase B1 von CBP-WP-020 wird der Pfad **`deployments/profile-a/`** als **kanonischer Repository-Ort für profilspezifische Deployment-Artefakte des Referenzprofils A** autorisiert. **B — Verhältnis zu ADR-0007:** ADR-0007 definiert `deployments/` bereits als Zielstruktur für „profilspezifische Vorlagen **ohne private Werte**"; D-055 setzt diesen **bereits angenommenen** Zielstrukturteil kontrolliert um — **es wird keine neue Architektur erfunden**. **C — Verhältnis zu D-029:** Die Autorisierung ist **ausschließlich additiv**. Verboten bleiben: Verschieben, Umbenennen oder Löschen bestehender Dateien, Umsortieren bestehender Verzeichnisse, vollständige Migration auf die ADR-0007-Zielstruktur sowie Verlust oder Umschreiben von Git-Historie. Das Anlegen eines **neuen, bislang nicht vorhandenen** Artefaktpfads ist **keine Migration bestehender Inhalte**; **D-029 bleibt vollständig wirksam**. **D — WP-020-Zielzustand:** CBP-WP-020 erzeugt ein Profil-A-Deployment-Bundle, sichere Konfigurationsvorlagen, Compose-Artefakte, Identitäts- und Privilegienabbildung, Mount- und Datenklassenverträge, Egress-Konfiguration, Secret-Referenzmodell, Installations-, Validierungs- und Rollbackrunbook, deterministische Offline-Validatoren sowie synthetische Negativkonfigurationstests. **E — Ausgeschlossener Scope:** reale VM, Installation, Containerstart, Proxmox-, NAS-, UniFi- und Netzwerkzugriff, lokale UID-/GID-Festlegung, konkrete IP-Adressen, Hostnamen und lokale Pfade, Secret-Werte, RT-2-Implementation, reale Persistenz, Backupausführung, Restore-Test, Security-Enforcement, Gateauswertung, Capability-Freigabe sowie Source- oder Mapping-Aktivierung. **F — RT-2-Grenze:** **P1 ist verbindlich** — WP-020 darf ausschließlich den **abstrakten Pfad-, Mount- und Schnittstellenvertrag** für ein späteres RT-2 definieren und **nicht** erzeugen: RT-2-Speicher, RT-2-Ereignisse, Hashverkettung, append-only Runtime, Retention-Engine, RT-2-Backup oder RT-2-Restore. **G — Security-Status:** WP-020 darf Konfigurationsvorlagen und Sollmodelle erstellen; **alle zwölf Security Controls bleiben `DOCUMENTED ONLY`**. **Kein** Control darf durch WP-020 auf `implemented`, `tested` oder `enforced` gesetzt werden. Die Offline-Validatoren prüfen **ausschließlich Artefakte und Verträge**, nicht die reale Durchsetzung auf Profil A. **H — Capability- und Gate-Grenze:** Capabilities bleiben **0/29**; Mapping Activation Gate und Security Foundation Readiness Gate bleiben **`NOT EVALUATED`**; **R-20 bleibt offen**. **I — ADR-Bewertung: `ADR_NOT_REQUIRED`** — ADR-0007 definiert den Zielpfad bereits; keine neue Trust Boundary, keine neue Produktabhängigkeit, keine neue Deployment-Topologie, keine neue Persistenzarchitektur, keine RT-2-Implementation, keine Änderung des Service-Identitätsmodells; die Verzeichnisanlage ist **additiv und reversibel** und migriert keine bestehenden Inhalte. **J — Risiko:** **keine neue Risiko-ID**. Bezüge: **R-01 steigt** durch neue Konfigurationsartefakte — Leakage- und Public-Neutrality-Tests in B2 sind **zwingend**; **R-20**, **R-25**, **R-31** und **R-32** bleiben offen; **R-33** bleibt in diesem uncommitteten Lauf unverändert. Zugleich wird der Phase-A-Zuschnitt bestätigt: **Zielzustand Z1**, **Scope S2**, **RT-2-Grenze P1**; **Phase B3 (reale Bereitstellung) ist ausdrücklich nicht Bestandteil von CBP-WP-020** und erfordert ein separates Work Package mit eigenem Human Gate | `accepted` | **A0** | Entscheidungsblock CBP-WP-020 | 2026-07-29 | Deployment-Artefaktstruktur Profil A | — (**ADR_NOT_REQUIRED**) |
| **D-056** | **CANONICAL SECURITY TEST INVENTORY AUTHORITY AND RECONCILIATION SCOPE.** **A — Kanonischer Wert:** Das Security-Foundation-Testinventar umfasst **32 aktive eindeutige Negativtests** — **NT-01 bis NT-24** (24 IDs) und **NT-26 bis NT-33** (8 IDs). **B — Positivtest:** Zusätzlich existiert **PT-01** als **ein** aktiver Positivtest. **C — Gesamtinventar:** **33 Security-Foundation-Testfälle** (32 NT + 1 PT). **D — NT-25:** **nicht aktiv**. Der frühere Fall war inhaltlich ein Positivtest und ist korrekt als **PT-01** klassifiziert; nach Regel **TT-5** wird eine umbenannte ID nicht neu vergeben — **NT-25 bleibt bewusst frei**. **E — NT-32 und NT-33:** **gültig, aktiv und aktuell**. Sie ersetzen die früheren Egress-Verwendungen der dokumentübergreifend doppelt vergebenen IDs NT-23 und NT-24; die ursprünglichen **NT-23- und NT-24-Fälle der Acceptance Matrix bleiben aktiv und unverändert**. **F — Zahl 31:** Der Wert **31 ist ein überholter, falsch etikettierter Ableitungswert**. Er entspricht dem von CBP-WP-011 auf **33** korrigierten **Gesamtwert** (30 NT + 1 PT) und wurde in einer nicht nachgeführten Zusammenfassungszeile fälschlich als „Negativtests" geführt; von dort ist er über die Roadmap in spätere Dokumente und ausführbare Artefakte gewandert. **31 ist keine konkurrierende Zählung.** **G — Authority-Rangfolge:** Authoritativ ist die **A2-Acceptance-Matrix**, die die IDs **definiert** und explizit auszählt; Spezifikation, Egress- und Evidence-Policy sind konsistent; Roadmap (A3), Work-Package-Dokumente und Statusspiegel sind **abgeleitet** und unterliegen der Matrix. **H — Zuständigkeit:** **CBP-WP-021 ist ausschließlich für die vollständige Reconciliation dieser Kennzahl zuständig.** **I — Reconciliation-Umfang:** Die Reconciliation umfasst ausdrücklich auch **ausführbare Artefakte** — `deployments/profile-a/bundle.json`, `deployments/profile-a/validate.py` und die zugehörigen Bundle-Tests. Sie ist damit **keine rein redaktionelle Änderung**. **J — Keine Control-Hochstufung:** Alle zwölf Security Controls bleiben **`DOCUMENTED ONLY`**. **K — Keine Testausführung:** **Kein** Security-Foundation-Test wird als ausgeführt gewertet — **0 von 32** Negativtests, **0 von 1** Positivtest. Die Feststellung eines Inventarwerts ist **keine** Testausführung und **keine** Gateauswertung. **L — Gates:** Mapping Activation Gate und Security Foundation Readiness Gate bleiben **`NOT EVALUATED`**. **M — Capabilities:** bleiben **0 von 29**. **N — R-20:** bleibt **offen**. **O — R-33:** bleibt in Phase B0 unverändert bei **17/20** und wird erst in **Phase C** fortgeschrieben. **P — Risiko:** **keine neue Risiko-ID**; der Vorgang fällt unter das bestehende **R-33**. **Q — ADR-Bewertung: `ADR_NOT_REQUIRED`** — keine neue Trust Boundary, keine neue Security-Architektur, kein neuer Repository-Root, keine neue Runtime- oder Persistenzarchitektur, keine Änderung des Service-Identitätsmodells; es wird ausschließlich ein bereits belegter Inventarwert festgestellt und fortgeschrieben. **R — KB-04:** **nicht Bestandteil von CBP-WP-021.** Der abgeschlossene KB-04-Phase-A-Bericht bleibt als vorbereitende Architekturgrundlage erhalten. **S — Folge-Work-Package:** Das spätere KB-04-Paket ist als **möglicher Kandidat CBP-WP-022 vorgemerkt**, aber **weder registriert noch autorisiert**. **T — Commitzähler-Governance:** Die Phase-A-Empfehlung **C3** ist **nicht** Bestandteil von D-056 und wird später als separate Governance-Note behandelt. **U — Commit-Autorität:** Commit und Push verbleiben **ausschließlich beim Human Maintainer** | `accepted` | **A0** | Entscheidungsblock CBP-WP-021 | 2026-08-03 | Kanonisches Security-Testinventar | — (**ADR_NOT_REQUIRED**) |
| **D-057** | **REGISTER CBP-WP-022 — KB-04 ENFORCEMENT STAGE 1.** **A — Gegenstand:** CBP-WP-022 wird als Work Package **registriert**; Titel **„KB-04 Enforcement Stage 1"**. **B — Kanonische Bedeutung von KB-04:** **Dateisystemrechte** — Ziel *„Deny-by-default auf Dateiebene"*, Bedrohung *„Direktzugriff unter Umgehung der Anwendung"*; Anforderung: explizite Owner- und Gruppenregeln, **keine world-writable Dateien**, **kein Schreibrecht auf Canonical durch Retrieval oder Ingest**, **Symlink-Escapes blockieren**, sichere Dateierstellung und **atomare Writes**. Quelle: [TECHNICAL_SECURITY_FOUNDATION_SPECIFICATION.md](../docs/security/TECHNICAL_SECURITY_FOUNDATION_SPECIFICATION.md) §KB-04 (A2, Grundlage **ADR-0009**, A1) und [SECURITY_CONTROL_ACCEPTANCE_MATRIX.md](../docs/security/SECURITY_CONTROL_ACCEPTANCE_MATRIX.md) §KB-04 (A2). **C — „Enforcement Stage 1":** bezeichnet **Stufe 1 der neunstufigen technischen Durchsetzungsreihenfolge** (Spezifikation §11) — **OS-Dateirechte**, *„gilt noch, wenn die Anwendung kompromittiert ist"*. **KB-04 ist die unterste tragende Ebene; versagt sie, sind KB-05 bis KB-07 wirkungslos.** Der englische Begriff „Enforcement Stage 1" ist eine **Benennung** dieser bestehenden Stufe, **keine neue Definition**. **D — Bestehende Registrierung:** KB-04 ist bereits als **Control** geführt (Acceptance Matrix), Status **`DOCUMENTED ONLY`**, Nachweisstufe **4**, Durchsetzungsstufe **1**; es ist Nachweisgegenstand der Readiness-Gate-Punkte **3** (Stufe 2) und **5** (Stufe 4, NT-04) und Voraussetzung der Mapping-Gate-Punkte 7, 8 und 11. Negativtests: **NT-04** (Canonical Write scheitert) und **NT-05** (Symlink Escape blockiert) — **beide nicht ausgeführt**. Stop-Bedingung **SB-S04**. **E — Voraussetzungen laut Repository:** **KB-01 und KB-02** ([TECHNICAL_SECURITY_FOUNDATION_PLAN.md](../docs/roadmap/TECHNICAL_SECURITY_FOUNDATION_PLAN.md), Umsetzungsreihenfolge Schritt 1: „KB-01, KB-02, KB-04 — Fundament"); die Nachweisstufe 4 verlangt eine **reale Profil-A-Instanz**, die **nicht existiert**. **F — Offene Decision:** **OD-37** (P1, Deployment Required) betrifft ausdrücklich die produktive Isolation auf der Ziel-VM für **KB-03 und KB-04** und bleibt **offen**. **G — Registrierungsgrenze:** D-057 registriert **ausschließlich** das Work Package und seine Authority-Grenze. **D-057 autorisiert keine technische Implementierung**, wählt **keine** Architektur und führt **kein** neues Runtime- oder Security-Enforcement-Verfahren ein. **H — Stage-1-Grenze:** Die vorhandene Authority liefert die **Anforderung** an KB-04, **keine** vollständige technische Stage-1-Lösungsdefinition. Stage 1 wird deshalb als **noch zu konkretisierende Implementierungsstufe** registriert; Design und ADR-Bewertung sind **Voraussetzung einer späteren B1-Freigabe**. **I — ADR-Status:** **`ADR_NOT_REQUIRED` gilt ausschließlich für diese Registration-Decision.** Die ADR-Erforderlichkeit der späteren technischen Umsetzung ist **vor** einer B1/B2-Freigabe anhand des dann konkretisierten Designs **erneut zu prüfen**; `ADR_NOT_REQUIRED` ist **keine generelle ADR-Befreiung**. **J — Unveränderte Grenzen:** zwölf Controls bleiben **`DOCUMENTED ONLY`** (**keine** Hochstufung durch diese Registrierung), beide Runtime-Gates bleiben **`NOT EVALUATED`**, Capabilities **0 von 29**, Security-Foundation-Tests **0 von 32** Negativ- und **0 von 1** Positivtest, **R-20 offen**, **R-33 unverändert 18/21**, **keine neue Risiko-ID**, keine reale Bereitstellung, kein RT-2. **K — Human-Maintainer-Authority:** Der Human Maintainer hat die **Registrierung** von CBP-WP-022 ausdrücklich freigegeben; **B1 und B2 sind nicht autorisiert**. **L — Folge-Work-Package:** **CBP-WP-023 ist nicht registriert und nicht autorisiert.** **M — Commit-Autorität:** Commit und Push verbleiben **ausschließlich beim Human Maintainer** | `accepted` | **A0** | Entscheidungsblock CBP-WP-022 | 2026-08-03 | KB-04 Enforcement Stage 1 — Registrierung | — (**ADR_NOT_REQUIRED** nur für die Registration) |
| **D-058** | **DETERMINE ADR REQUIREMENT FOR CBP-WP-022 — KB-04 ENFORCEMENT STAGE 1. Ergebnis: `ADR_REQUIRED`.** **A — Gegenstand:** Verbindliche Feststellung, ob vor einer technischen Umsetzung von KB-04 Enforcement Stage 1 ein **neuer ADR** erforderlich ist. **B — Ergebnis:** **`ADR_REQUIRED`.** Vor jeder technischen Implementierung ist ein neuer ADR zu erstellen; die voraussichtliche nächste Kennung ist **ADR-0014**. **ADR-0014 wurde in diesem Lauf nicht angelegt.** **C — Berücksichtigte Authority:** **ADR-0009** (A1) und die Spezifikation §KB-04 (A2) legen die **Anforderung** fest — explizite Owner- und Gruppenregeln, keine world-writable Dateien, kein Schreibrecht auf Canonical durch Retrieval oder Ingest, Symlink-Escapes blockieren, sichere Dateierstellung und atomare Writes; die **Acceptance Matrix** (A2) bindet NT-04/NT-05 und Nachweisstufe 4; das **Identity Model** (A2) fixiert V-1 (kein root), V-3 (kein privilegierter Container), V-5 (kein Canonical-Schreiben ohne dokumentierte Freigabe), V-11, V-12 sowie M-1 bis M-4; **ADR-0010/ADR-0011** setzen den bestehenden Präzedenzfall atomarer Schreibweise (exklusive Temp-Datei, `fsync`, `os.replace`, kein Schreiben außerhalb des Roots, keine Hard- oder Symlinks). **D — Offene Designachsen, die keine bestehende Authority entscheidet:** (1) **Owner- und Gruppenmodell** — das Identity Model führt ausdrücklich *keine konkreten Unix-Benutzer, keine Gruppen, keine UID- oder GID-Werte*; (2) **UID-/GID-Abbildung und Host-/Container-Verantwortungsgrenze** — Spezifikation und Identity Model führen Unix-/Container-Identitäten, UID und GID als **Deployment Required**; (3) **konkrete Datei- und Verzeichnismodi** — im Foundation Plan ausdrücklich *offen — Deployment*; (4) **Durchsetzungsakteur** — Setup-, Deployment- oder Runtime-Verantwortung ist nirgends festgelegt; (5) **Verifikations- gegen Korrektursemantik** — ob eine spätere Umsetzung Rechte nur **prüft und fail-closed ablehnt** oder sie **aktiv setzt**, ist nicht entschieden; (6) **Migrations- und Reparaturverhalten** für bestehende Artefakte mit falschen Rechten ist nicht entschieden. **E — Reversibilität:** **gering.** Besitz- und Modusentscheidungen wirken auf persistente Artefakte in Canonical Store, Quarantine Store, Registry und Freigabebereich; eine nachträgliche Änderung erfordert eine rekursive Korrektur über bestehende Bestände und berührt die Host-/Container-Grenze. **F — Sicherheitsauswirkung:** **hoch.** KB-04 ist die **unterste tragende Ebene**; versagt sie, sind KB-05 bis KB-07 wirkungslos. Eine Fehlentscheidung beim Owner-, Gruppen- oder Durchsetzungsmodell würde die gesamte darüber liegende Kontrollkette entwerten. **G — Scope-Auswirkung:** **komponentenübergreifend** — betroffen sind Quarantine Store, Source Registry, Canonical Store, Freigabebereich, Profil-A-Bundle und der spätere Deploymentprozess. **H — Bewertung der zehn Prüffragen:** neue Architektur- beziehungsweise Sicherheitsmusterwahl **ja**; Wahl zwischen mehreren tragfähigen Owner-/Gruppenmodellen **ja**; Deploymentmodell teilweise fixiert (V-1/V-3 verbieten root), die **Privilegierung des Setup-Akteurs** jedoch **offen**; dauerhafte Host-/Container-Verantwortungsgrenze **festzulegen**; neuer interner Vertrag **entsteht**; Migrations-, Kompatibilitäts- und Deploymentwirkung **ja**; schwer reversibel **ja**; mehrere Komponenten **ja**; **ADR-0009 ist nicht konkret genug**, um die technische Lösung eindeutig zu bestimmen; **bestehende ADRs decken die Lösungswahl nicht ab**. **I — Nächste Phase:** **B1B — ADR-0014 Authoring and Design Decision.** **B1B ist durch diese Decision nicht autorisiert** und bedarf einer eigenen Freigabe. **J — Implementierungsverbot:** **D-058 autorisiert keine technische Implementierung.** **B2 bleibt gesperrt.** Kein Runtime-Code, keine Tests, keine Bundle- oder Validatoränderung. **K — Keine Architekturvorwegnahme:** D-058 trifft **keine** Auswahl unter den offenen Designachsen; sie stellt ausschließlich fest, **dass** eine solche Auswahl per ADR zu treffen ist. **L — Unveränderte Grenzen:** **KB-04 bleibt `DOCUMENTED ONLY`**, alle zwölf Controls bleiben `DOCUMENTED ONLY`, beide Runtime-Gates bleiben `NOT EVALUATED`, Capabilities **0 von 29**, **NT-04 und NT-05 nicht ausgeführt** (**0 von 32** Negativtests, **0 von 1** Positivtest), **R-20 offen**, **OD-37 offen**, **R-33 unverändert 18/21**, **keine neue Risiko-ID**, keine reale Bereitstellung, kein RT-2. **M — Commit-Autorität:** Commit und Push verbleiben **ausschließlich beim Human Maintainer** | `accepted` | **A0** | Entscheidungsblock CBP-WP-022 | 2026-08-03 | KB-04 Enforcement Stage 1 — ADR-Gate | **ADR_REQUIRED** — ADR-0014 vorgesehen, nicht angelegt |
| **D-059** | **ADOPT ADR-0014 — KB-04 STAGE 1 FILESYSTEM ENFORCEMENT ARCHITECTURE. Ergebnis: `ADR-0014_ACCEPTED`.** **A — Gegenstand:** Annahme der Architekturentscheidung für die Durchsetzung der Dateisystemrechte (KB-04) in Stufe 1, in Erfüllung der durch **D-058** (A0) verbindlich festgestellten Feststellung **`ADR_REQUIRED`**. **B — Autoritätszuordnung:** **D-059 trägt Decision Class A0** — ausdrücklicher Human-Maintainer-Beschluss. **ADR-0014 trägt nach Annahme Autoritätsklasse A1**. Das bestehende Autoritätsmodell (A0 = ausdrücklicher Human-Maintainer-Beschluss, A1 = Release, Tag oder angenommener ADR) bleibt **unverändert**; es wird **keine neue Konvention eingeführt**. **C — Angenommener ADR:** [ADR-0014-kb-04-stage-1-filesystem-enforcement.md](../docs/decisions/ADR-0014-kb-04-stage-1-filesystem-enforcement.md), Status **`accepted`**, Datum **2026-08-03**, Scope **CBP-WP-022 — KB-04 Enforcement Stage 1**. **D — Ausgewählte Architektur:** **Host-authoritative Enforcement mit deklarativem Zielmodell und read-only Runtime-Validierung** (Option A). Drei Schichten mit disjunkten Befugnissen: das **Repository** beschreibt ausschließlich das **abstrakte Zielmodell** — Bereiche, Rollen, Zugriffsart, Rechteprofilklassen, Invarianten, Prüfregeln — **ohne reale Identitäten, UIDs, GIDs, Modi, Benutzer, Gruppen oder Hostpfade**; der **Deployment- und Setup-Akteur** bindet Rollen an konkrete Identitäten, **setzt** Besitz und Rechte **vor** dem Runtime-Start und **repariert** ausschließlich in einem ausdrücklich aufgerufenen Modus; die **Runtime** besitzt **ausschließlich Lese- und Ablehnungsrecht** — sie validiert und **scheitert fail-closed**. **E — Kategorische Kernregel:** **Keine lang laufende Runtime-Komponente verändert jemals Besitz, Gruppe, Modus oder Identität** — weder beim Start, noch im Betrieb, noch zur Fehlerbehebung, und unabhängig davon, ob die Umgebung die nötigen Privilegien böte. **F — Repository-gestützte Begründung:** Die Spezifikation führt **Dateimodi, Owner und Gruppen als Deployment Required**; das Identity Model führt dieselben Werte unter *Offene Deploymentwerte* mit dem Nachsatz, dass **keiner dieser Werte dort festgelegt wird**; das Installation Runbook verlangt, dass UID und GID **im lokalen Operator-Workspace außerhalb des Repositorys** befüllt und **niemals zurückkopiert** werden; das committete Profil-A-Bundle führt KB-04 bereits als **`filesystem-permission-target-model`** und entzieht der Runtime mit **`cap_drop: ALL`**, `no-new-privileges` und `read_only` jede Rechteprivilegierung; **V-1** und **V-3** verbieten root und privilegierte Container. **G — Verworfene Optionen:** **Option C (Runtime Self-Repair)** — **repository-widersprüchlich**: verlangt durch V-1/V-3 und das Bundle entzogene Privilegien, verletzt **I-7**, weil sie eine Abweichung **behebt statt abzulehnen**, und entwertet **NT-04/NT-05**, deren Aussagekraft davon abhängt, dass ein falscher Zustand bestehen bleibt und auffällt. **Option D (ACL-centric)** — als Hauptmechanismus verworfen: zusätzliche Werkzeug-, Dateisystem- und Plattformabhängigkeit entgegen der stdlib-only-Praxis, während die Spezifikation die Anforderung in Owner-, Gruppen- und world-writable-Begriffen formuliert; eine ACL-Ebene darf später **ergänzend** härten, **nie ersetzen**. **Option E (Zielmodell ohne Runtime-Validierung)** — verworfen: kein Nachweis, keine Fail-closed-Wirkung, kein Unterschied zum heutigen `DOCUMENTED ONLY`. **Option B (Privileged Bootstrap)** — **nicht verworfen, sondern verortet**: die Initialisierungsverantwortung ist Teil der gewählten Architektur und liegt **vollständig auf der Deployment-Seite der Grenze**, niemals als privilegierte Komponente innerhalb der Bereitstellungseinheit. **H — Entschiedene Modelle:** Authority-Modell, Identitätsmodell mit expliziter, beim Start gegen die **effektive** Identität geprüfter Host-/Container-Bindung, **Rechteprofil-Modell PP-1 bis PP-4** (owner-write · owner-write mit group-read · service-read-only · not-present) mit acht kategorischen Klassenregeln, Initialisierung, Validierung an vier Zeitpunkten, Migration und Reparatur nach dem Prinzip **Plan vor Wirkung**, Link- und Pfadsicherheit einschließlich ausdrücklich anerkannter **TOCTOU**-Grenze, Plattformgrenze und Nachweisgrenze. **Die sieben Sicherheitsinvarianten I-1 bis I-7 aus Phase B1A bleiben unverändert bindend.** **I — Bewusst nicht entschieden:** vollständige Pfad-zu-Rolle-zu-Modus-Matrix, exakte Modusprofile, `umask`-Wert, Bereichszuordnung der Profilklassen, Konfigurationsschema, Identitätsvalidierungs-, Initialisierungs-, Validierungs-, Migrations- und Reparaturvertrag, Fehlerklassen, **Issue- und Exitcodes (in ADR-0014 bewusst keine festgelegt)**, Testmatrix und NT-04-/NT-05-Abbildung, B2-Datei- und Implementierungsscope sowie **sämtliche realen UID-, GID-, Benutzer- und Gruppenwerte** — vierzehn offene Implementierungsparameter. **J — Nachfolgende Contract-Finalisierung:** **B1C — Enforcement Contract and Validation Plan**. **B1C ist durch diese Decision nicht autorisiert** und bedarf einer eigenen Freigabe. **K — Phasenstand:** **B1B ist abgeschlossen.** **B2 ist ausdrücklich nicht autorisiert**; ohne B1C ist keine technische Implementierung zulässig. **L — Implementierungsverbot:** **D-059 enthält keine Implementierungsfreigabe.** Kein Runtime-Code, keine neue Python-Datei, keine Tests, keine Testausführung, keine Bundle- oder Validatoränderung, kein Containerstart, keine Bereitstellung, keine Rechtevergabe, keine realen UID-/GID-Werte. **M — Unveränderte Grenzen:** **KB-04 bleibt `DOCUMENTED ONLY`**, alle zwölf Controls bleiben `DOCUMENTED ONLY` — **keine Control-Hochstufung**; beide Runtime-Gates bleiben **`NOT EVALUATED`** — **keine Gateauswertung und keine Gatefreigabe**; Capabilities **0 von 29**; **NT-04 und NT-05 nicht ausgeführt** (**0 von 32** Negativtests, **0 von 1** Positivtest); **SB-S04 nicht wirksam**; **R-20 offen**; **OD-37 offen** — durch diesen ADR **strukturiert, nicht geschlossen**; **R-33 unverändert 18/21**; **keine neue Risiko-ID**; keine reale Bereitstellung; kein RT-2; **CBP-WP-023 nicht registriert**. **Eine entschiedene Architektur ist keine Sicherheitswirkung.** **N — Commit-Autorität:** Commit und Push verbleiben **ausschließlich beim Human Maintainer** | `accepted` | **A0** | Entscheidungsblock CBP-WP-022 | 2026-08-03 | KB-04 Enforcement Stage 1 — Architekturentscheidung | **ADR-0014** (A1) — `ADR-0014_ACCEPTED` |
| **D-060** | **ADOPT KB-04 STAGE 1 ENFORCEMENT CONTRACT AND VALIDATION PLAN. Ergebnis: `KB-04_STAGE_1_CONTRACT_ACCEPTED`.** **A — Gegenstand:** Annahme des implementierungsfähigen Enforcement-Vertrags für **KB-04 Enforcement Stage 1**, niedergelegt in [KB_04_STAGE_1_ENFORCEMENT_CONTRACT.md](../docs/security/KB_04_STAGE_1_ENFORCEMENT_CONTRACT.md) (Status **accepted contract**, 2026-08-03). **B — Verhältnis zu ADR-0014:** **D-060 konkretisiert ADR-0014.** **D-060 ändert ADR-0014 nicht** und **führt keine neue Architektur ein**; der Vertrag definiert ausschließlich die implementierungsfähige Ausgestaltung **innerhalb der bereits `accepted` Architektur** — Host-authoritative Enforcement mit deklarativem Zielmodell und read-only Runtime-Validierung. **ADR-0014 bleibt die bindende A1-Authority.** **C — ADR-Status: `ADR_NOT_REQUIRED`.** Dies gilt **ausschließlich, solange der Contract vollständig innerhalb ADR-0014 bleibt**. **Jede spätere Abweichung von ADR-0014 benötigt eine neue ADR-Erforderlichkeitsprüfung.** **D — Gebundene Parameter (die vierzehn offenen Punkte aus ADR-0014):** **elf abstrakte Pfadklassen** **PC-01** bis **PC-11**, verankert an den bereits committeten `container_paths` und `tmpfs_targets` des Profil-A-Bundles, **ohne einen erfundenen Rootpfad und ohne Hostpfad**; **zehn Akteure** mit zwölf Befugnisspalten; **Rechteprofile** **PP-1** `0600`/`0700`, **PP-2** `0640`/`0750` (setgid `2750` nur bei zwingender Gruppenvererbung), **PP-3a** `0640`/`0750`, **PP-3b** `0444`/`0555` — ein **eng begrenztes Kompatibilitätsprofil**, das **ausschließlich für PC-07** gilt, **nicht auf andere Pfadklassen übertragbar** ist, **nicht** als allgemeines read-only Profil verwendet werden darf und unter dem **Secrets, Tokens, Passwörter, private Schlüssel, Credential-Werte, konkrete lokale UID-/GID-Werte, konkrete lokale Benutzer- oder Gruppennamen, vollständige Identitätsbindungen, sensible Operator- oder Deploymentwerte und private Hostpfade ausdrücklich unzulässig** sind (Regeln 3b-1 bis 3b-16); ein solches Artefakt ist **nicht als PP-3b klassifizierbar**, wird **nicht still übernommen** und endet ohne sichere Alternativklassifikation **fail-closed**. **`0444`/`0555` ist kein sicherer Default** — der Grundzustand bleibt **kein World-Zugriff**; die Ausnahme beruht **ausschließlich** auf dem committeten Bundlevertrag, **schwächt keine andere Profilklasse und keine Invariante ab**, und **`world-writable` bleibt ausnahmslos verboten**. Eine spätere Verschärfung von PC-07 bleibt zulässig und ist **kein Vertragsbruch**, sofern die erforderliche Runtime-Lesbarkeit erhalten bleibt. **Dieser Vertrag ändert das Bundle nicht**, sondern **übernimmt und validiert** den Wert. Hinzu kommt **PP-4** *not-present*; `umask` **`0077`** beziehungsweise **`0027`**; acht kategorische Klassenregeln (kein World-Schreibbit, kein World-Lesebit außerhalb PP-3b, kein setuid/setgid auf Dateien, Execute nur zur Verzeichnisdurchquerung, keine ACL als Ersatz, Konformität **bei der Erstellung**, **nur Bindungswerte konfigurierbar**, kein Default und kein Fallback). **E — Mount- und POSIX-Trennung (MT-1 bis MT-8):** POSIX-Rechte ersetzen keinen Mountmodus und umgekehrt; **Runtime-Schreibzugriff verlangt gleichzeitig** passenden Mountmodus, passende Rolle, passendes PP-Profil und **positive** Identitätsbindung; read-only ist **doppelt** durchzusetzen; eine Gruppenmitgliedschaft überschreibt niemals einen read-only Mount; **unbekannter Mountstatus = fail-closed**. Host- und Runtime-/Container-Berechtigungen sind **vier getrennte Prüfdimensionen** — **D-I** Host-Quellobjekt, **D-II** Mountvertrag, **D-III** Runtime-sichtbares Objekt, **D-IV** Runtimeidentität (MT-9 bis MT-14): **ein im Bundle deklarierter Modus ist kein Nachweis der Host-Quellrechte**, **D-III belegt D-I nicht** und **D-I belegt D-III nicht**; **eine nicht prüfbare Dimension gilt als nicht erfüllt**, und der Gesamtzustand ist **nur konform, wenn alle erforderlichen Dimensionen positiv validiert** wurden. **Keine Bundle-Datei wurde geändert.** **F — Identitätsbindungsvertrag:** implementierungsneutrales Schema mit elf Feldern, davon **zehn Pflichtfelder**; `value_origin` lässt **ausschließlich** `operator-workspace` zu; **keine Konfigurationsdatei angelegt, kein realer Wert eingetragen**; **keine sicheren Defaults, keine stille Ableitung, keine Fallback-Identität**; fehlende, doppelte, kollidierende, unbekannte, nicht auflösbare oder abweichende Bindung sowie unerwartete Supplementary Group und unerwartete effektive Identität enden **sämtlich fail-closed**. **G — Initialisierung und Bestand strikt getrennt:** Apply ist **nur** auf einer nachweislich neuen, leeren Zielstruktur zulässig und nur bei gleichzeitiger Erfüllung **aller sechs** Voraussetzungen; sobald Bestand, unbekannte Dateien oder abweichende Rechte vorliegen, ist der Vorgang **keine Initialisierung, sondern Migration** — **plan-only**. Transaktionsgrenze **Preflight → Plan → begrenztes Apply → Post-Validation**; Teilerfolg und nicht feststellbarer Zustand sind fail-closed; **es wird ausdrücklich keine Rollback-Zusage gegeben**, weil kein Mechanismus existiert. **H — Validierung an vier Zeitpunkten, sämtlich read-only:** Installation, **Start vor Dienstaufnahme** (Fehler → Startsperre, bestehender Exitcode `RUNTIME_START_BLOCKED` 4), Schreibzeit und Gate-Zeitpunkt; Laufzeitprüfungen sind nur als ausdrücklich angestoßene read-only Wiederholung zulässig — **kein periodischer Self-Repair, keine nebenläufige Rechteänderung**. Grundregel: **nicht feststellbar = nicht erfüllt**. **I — Link- und Pfadsicherheit (LP-1 bis LP-10):** normalisierte Auflösung, feste Root-Boundary, bereichsverlassende Symlinks unzulässig, **interne Symlinks werden abgelehnt statt aufgelöst**, **Hardlinks in geschützten Bereichen unzulässig**, Device Files, FIFOs und Sockets unzulässig, Mount- und Bind Mounts kein zulässiger Pfadbestandteil, **kein Vertrauen auf String-Präfixe**, **TOCTOU ausdrücklich anerkannt** — ein Prüfergebnis gilt nicht über einen Schreibvorgang hinaus, und die Rechteprüfung ist **niemals** die einzige Absicherung. **J — Migration und Reparatur:** Migration ist ein bewusster Operatorvorgang nach dem Prinzip **Plan vor Wirkung**, ohne implizite Rekursion und ohne stille Übernahme unsicherer Zustände; **Reparatur ist niemals Runtime-Self-Repair, niemals still, niemals nebenläufig**, verlangt eine Auditspur, diese Auditspur ist **RT-2**, **RT-2 ist nicht implementiert** — **der ausführende Reparaturmodus bleibt gesperrt**. **K — Fehlerklassen:** **vierundzwanzig** implementierungsneutrale Klassen mit Präfix **`KB04-`**, jeweils mit Semantik, Installations-, Start-, Laufzeit- und Gatewirkung, Reparierbarkeit, Operatoraktion und Evidenzbedarf. **Alle sind fail-closed**; „plan-only“ bedeutet ausdrücklich: ein Plan darf entstehen, die Ausführung bleibt gesperrt. **L — Issue- und Exitcodes:** Audit ergab die Präfixe `BND`, `MAP`, `GATE`, `QF`, belegte Exitcodes `0`, `2`–`14`, `64`, `70` und **`KB04` als kollisionsfrei (0 Treffer)**. Entscheidung **gemischt**: **Möglichkeit A** für Exitcodes — Wiederverwendung von `RUNTIME_START_BLOCKED` (4), `CONFIG_INVALID` (2) und `USAGE_ERROR` (64) **ohne semantische Überladung**; **Möglichkeit B** für zwei **reservierte, nicht implementierte** Namen `FILESYSTEM_ENFORCEMENT_BLOCKED` (**15**) und `FILESYSTEM_MIGRATION_REQUIRED` (**16**) sowie für die vierundzwanzig `KB04-`-Issue-Codes. **Keine bestehende Nummer oder Kennung wurde überschrieben**, **keine neue Security-Test-ID erzeugt**, **NT-25 bleibt nach Regel TT-5 frei und wird nicht wiederverwendet**; die Reservierung ist durch ADR-0014, offener Parameter 10, ausdrücklich gedeckt. **Bedeutung der Reservierung (RC-1 bis RC-10):** die Codes sind **ausschließlich Contract-Reservierungen**, **nicht implementiert**, **kein aktueller CLI-, Validator-, Gate- oder Runtimepfad emittiert sie**, **bestehende Codes werden nicht umgedeutet**, und **das heutige öffentliche Verhalten bleibt unverändert**. **B2 muss Implementierung, Dokumentation und Tests gemeinsam liefern**; **vor einer Implementierung sind Nummernfreiheit, Namenskollisionsfreiheit, Semantikeindeutigkeit und öffentliche Kompatibilität erneut zu prüfen**; bei einer Kollision gilt **keine stille Neunummerierung, keine semantische Überladung, B2 stoppen**. **Die bloße Reservierung ist kein Implementierungs- und kein Releaseversprechen**, und **es wird keine Security-Test-ID daraus abgeleitet**. **M — Validierungs- und Testplan:** **zwölf positive** und **dreiunddreißig negative** Testfälle — einschließlich der vollständigen PP-3b-Prüffolge über alle vier Dimensionen und der acht negativen PP-3b-Fälle (Secretmaterial, Verwendung außerhalb PC-07, nicht feststellbare Hostquelle, read-only Runtimesicht ohne positiv validierte Hostquelle, read-write Mount, veränderbares Artefakt, unerwartete Identität, Abweichung zwischen Bundlemodus und sichtbarem Zustand) mit den **vorläufigen internen Kennungen `KB04-T-P*` und `KB04-T-N*`, die ausdrücklich keine Security-Test-IDs sind**; synthetische und reale Evidenz sind getrennt — **nur real** sind `KB04-T-N07`, `KB04-T-N08` (**NT-04**) und `KB04-T-N14` (**NT-05**) sowie jede Rechteauflistung vor und nach dem Start. **Kein Testfall wurde ausgeführt.** **N — NT-04, NT-05, SB-S04, OD-37 und beide Gates:** wörtlich aus der bestehenden Authority abgebildet, ohne Umdeutung und ohne neue NT-ID; für jeden Fall ist festgehalten, **welche Aussage ein bestandener Test nicht treffen darf**. **SB-S04 bleibt nicht wirksam**, **OD-37 bleibt offen** — der Vertrag **strukturiert** die Vertragsbestandteile, schließt OD-37 aber **nicht**, weil dieser den Nachweis auf der Ziel-VM verlangt und ein Vertrag kein Nachweis ist; **beide Gates bleiben `NOT EVALUATED`**. **O — B2-Kandidat:** vier Teilphasen **B2A** (Contract Model and Read-only Validator), **B2B** (New-target Initialization Boundary), **B2C** (Synthetic Tests and Evidence) und **B2D** (Profile-A Deployment Integration) sind beschrieben; gesperrt bleiben produktive Reparatur, Migration bestehender Daten, RT-2, reale Gatefreigabe, Control-Uplift und Stage 2. **D-060 autorisiert keine dieser Teilphasen.** **B2 darf PP-3b nur übernehmen, wenn die vier Dimensionen getrennt validierbar sind, die Secretfreiheit vertraglich prüfbar ist und keine Dimension still übersprungen wird**; andernfalls gilt **kein stiller Fallback, kein bloßer Warnmodus, keine automatische Akzeptanz des Bundlewerts** — **B2 stoppt mit einem klaren Blocker** und eine Architektur- oder Contract-Neubewertung wird erforderlich. **P — Implementierungsverbot:** **D-060 autorisiert keine technische Implementierung** und **autorisiert B2 nicht**. Kein Runtime-Code, keine neue Python-Datei, keine Tests, keine Testausführung, keine Bundle- oder Validatoränderung, keine Config-Datei, keine Deploymentänderung, keine realen UID-, GID-, Benutzer-, Gruppen- oder Hostpfadwerte. **Q — Keine Statusveränderung:** **D-060 wertet keinen Gate-Status aus** und **stuft KB-04 oder einen anderen Control nicht hoch**. **KB-04 bleibt `DOCUMENTED ONLY`**, alle zwölf Controls bleiben `DOCUMENTED ONLY`, beide Runtime-Gates bleiben `NOT EVALUATED`, Capabilities **0 von 29**, **NT-04 und NT-05 nicht ausgeführt** (**0 von 32** Negativtests, **0 von 1** Positivtest), **SB-S04 nicht wirksam**, **R-20 offen**, **OD-37 offen**, **R-33 unverändert 18/21**, **keine neue Risiko-ID**, **RT-2 nicht implementiert**, keine reale Bereitstellung, **CBP-WP-023 nicht registriert**. **Ein implementierungsfähiger Vertrag ist keine Implementierung, und eine geplante Prüfung ist kein Nachweis.** **R — Human-Maintainer-Authority:** **B1C Contract and Validation Plan authorized**; **B2 ist nicht autorisiert**. **S — Commit-Autorität:** Commit und Push verbleiben **ausschließlich beim Human Maintainer** | `accepted` | **A0** | Entscheidungsblock CBP-WP-022 | 2026-08-03 | KB-04 Enforcement Stage 1 — Enforcement Contract | **ADR_NOT_REQUIRED** — konkretisiert **ADR-0014** (A1), ändert es nicht |
| **D-061** | **SELECT B2C SCOPE FOR CBP-WP-022 — SYNTHETIC TESTS AND EVIDENCE. Ergebnis: `B2C_TRACEABILITY_AND_NT_PREPARATION_SELECTED`.** **A — Entscheidungsanlass:** Der read-only Audit **B2C.0** ergab **`DECISION REQUIRED`**. Drei Befunde: (1) das Wort **„Evidence“** im Phasennamen „Synthetic Tests and Evidence“ (Contract §18) ist in der bindenden Scopespalte **nicht definiert** — dort stehen ausschließlich *Unit- und Contract-Tests*, *negative Fixtures* und *Vorbereitung von NT-04/NT-05*; (2) **zwei der drei Scopepunkte sind bereits geliefert** — **326 KB-04-Testmethoden** in sechs Modulen samt umfangreicher Negativfixtures aus B2A und B2B-P; (3) die **Eingabefläche des Gate-Evaluators** hätte durch ein Security-Control-Form-Artefakt implizit erweitert werden können — KB-04 ist runtime-scoped mit den Bindungen **(7, KB-04)**, **(8, KB-04)** und **(11, KB-04)**. Eine solche Erweiterung darf **nicht** als Nebenwirkung einer Testphase entstehen. **B — Gewählte Variante: T — Test-, Traceability- und NT-Preparation-Lesart.** B2C ist eine **ausschließlich synthetische** Test-, Fixture- und Rückverfolgbarkeitsphase; sie erzeugt **keinen mutierenden Produktionscode** und **keine reale Deploymentausführung**. **C — Abgelehnte Variante: E** — Security-Control-Form- beziehungsweise Gate-Evidence-Artefakt. **Variante E ist für CBP-WP-022 nicht autorisiert.** **D — Traceability-Basis:** Die **45** Contract-Testkennungen aus Contract §15 — `KB04-T-P01` bis `KB04-T-P12` und `KB04-T-N01` bis `KB04-T-N33` — bilden die vollständige spätere Nachweisbasis. Sie kommen im gesamten Test- und Produktionscode derzeit **null Mal** vor; die Abdeckung ist damit **nicht belegbar**. **E — Aufteilung:** **39 synthetisch abdeckbare Fälle** müssen später nachvollziehbar als **synthetisch abgedeckt** belegt werden; **sechs ausschließlich real ausführbare Fälle** werden in B2C **nur deklarativ vorbereitet** und **niemals als bestanden ausgegeben**. **F — Die sechs real-only Fälle sind exakt:** **`KB04-T-N07`**, **`KB04-T-N08`** (beide **NT-04**), **`KB04-T-N14`** (**NT-05**), **`KB04-T-N31`**, **`KB04-T-N33`** sowie die **reale Dimension D-I von `KB04-T-P12`**. **Ihre tatsächliche Ausführung bleibt B2D.** **G — Aussagegrenze, verbindlich:** **Eine Vorbereitung ist kein Nachweis.** **Ein Fixture ist keine NT-Ausführung.** **Eine synthetische Abdeckung ist keine operative Evidenz.** **H — Zulässige Nachweise:** synthetische Unit-, Contract- und statische Integrationsnachweise. **I — Ausgeschlossene Nachweise:** **keine** reale Linux-/POSIX- oder Profil-A-Evidenz, **keine** Ausführung von NT-04 oder NT-05, **keine** Nachweisstufe 4. **J — Evidence- und Gategrenze:** B2C erzeugt **kein Security-Control-Form-Artefakt**, **keinen Evidence-Schema-3.0-Producer** und **keine Gate-Eingabe**; es wertet **keinen Gatezustand aus** und verändert keinen. Ergänzend gilt unverändert die Schutzregel aus **ADR-0013**: ein Security-Control-Formartefakt ist **negative-evidence-only** und erfüllt **niemals** ein Gatekriterium. **K — Keine Hochstufung:** **KB-04 bleibt `DOCUMENTED ONLY`**; **OD-37 wird nicht geschlossen**, **SB-S04 nicht wirksam gesetzt**, keine Control- und keine Capability-Änderung. **L — Technische Grenze:** B2C erzeugt **kein neues Enforcement-Produktionsmodul** und benötigt **keine CLI-, Config- oder Deploymentänderung** sowie **keine neuen ReasonCodes** — die **24** registrierten KB04-Codes genügen. **Exitcodes 15 und 16 bleiben reserviert und nicht implementiert.** **M — Unabhängigkeit von B2B-Apply:** B2C ist **unabhängig** vom gesperrten Apply-Pfad und darf ohne ihn fortgesetzt werden; NT-04 und NT-05 verlangen eine **reale Profil-A-Instanz**, keine Apply-Funktion. Die Kette lautet **B2C → B2D**. **B2B-Apply bleibt nicht autorisiert.** **N — ADR-Gate: `ADR_NOT_REQUIRED`.** Alle zehn Prüffragen negativ: keine veränderte Verortung des Durchsetzungsakteurs, kein mutierender Code, keine Runtimekopplung, keine CLI-, Config- oder Deploymentsemantik, keine neue öffentliche API, keine Rollback- oder Reparaturzusage, keine irreversible Architekturentscheidung. **ADR-0014 bleibt unverändert. D-060 bleibt unverändert.** **O — Spätere Evidenzintegration:** Eine KB-04-Security-Control-Form- oder Gate-Evidence-Integration benötigt eine **eigenständige A0-Entscheidung** und ist **kein Bestandteil dieser B2C-Phase**. **P — Freigabegrenze:** **D-061 legt ausschließlich die Lesart fest.** Die technische **B2C-T-Implementierung ist durch diese Decision nicht autorisiert**; **B2D bleibt nicht autorisiert**; **CBP-WP-023 bleibt nicht registriert**. **Q — Unveränderte Grenzen:** beide Runtime-Gates **`NOT EVALUATED`**, alle zwölf Controls `DOCUMENTED ONLY`, Capabilities **0 von 29**, **NT-04 und NT-05 nicht ausgeführt**, **R-20 offen**, **OD-37 offen**, **R-33 unverändert 18/21**, **keine neue Risiko-ID**, **keine neue Control-ID**, **keine neue Security-Test-ID**, **RT-2 nicht implementiert**, keine reale Bereitstellung. **R — Commit-Autorität:** Commit und Push verbleiben **ausschließlich beim Human Maintainer** | `accepted` | **A0** | Entscheidungsblock CBP-WP-022 | 2026-08-04 | KB-04 Enforcement Stage 1 — B2C-Scope | **ADR_NOT_REQUIRED** — innerhalb **ADR-0014** (A1) und **D-060** |

> **D-061 legt eine Lesart fest, sie implementiert und autorisiert nichts.** Für
> **B2C** gilt verbindlich **Variante T**: eine **ausschließlich synthetische**
> Test-, Fixture- und Rückverfolgbarkeitsphase. Die **45** Contract-Testkennungen
> aus §15 bilden die spätere Nachweisbasis — **39** synthetisch abdeckbar,
> **sechs** ausschließlich real und damit **B2D** (`KB04-T-N07`, `KB04-T-N08`,
> `KB04-T-N14`, `KB04-T-N31`, `KB04-T-N33` sowie die Dimension **D-I** von
> `KB04-T-P12`). **Variante E — ein Security-Control-Form- oder
> Gate-Evidence-Artefakt — ist für CBP-WP-022 nicht autorisiert**; eine
> spätere Integration verlangt eine eigene A0-Entscheidung. **Eine Vorbereitung
> ist kein Nachweis, ein Fixture keine NT-Ausführung, eine synthetische Abdeckung
> keine operative Evidenz.** **B2C-T ist nicht autorisiert**, ebenso wenig B2B-Apply
> und B2D. **KB-04 bleibt `DOCUMENTED ONLY`**, beide Gates `NOT EVALUATED`,
> Capabilities **0 von 29**, NT-04 und NT-05 **nicht ausgeführt**, **R-20 offen**,
> **OD-37 offen**, **R-33 18/21**.

> **D-060 konkretisiert eine Architektur, sie ändert und autorisiert nichts.** Der
> KB-04-Stage-1-Enforcement-Vertrag bindet die vierzehn offenen Parameter aus
> **ADR-0014**: elf Pfadklassen **PC-01** bis **PC-11**, zehn Akteure, die Profile
> **PP-1** `0600`/`0700`, **PP-2** `0640`/`0750`, **PP-3a** `0640`/`0750`, **PP-3b**
> `0444`/`0555` (**eng begrenztes Kompatibilitätsprofil, nur PC-07, secretfrei,
> ausdrückliche Ausnahme statt sicherer Default**) und **PP-4** *not-present*,
> `umask` `0077` beziehungsweise `0027`, den Identitätsbindungsvertrag, die
> Trennung von Initialisierung und Migration, vier read-only Validierungszeitpunkte,
> zehn Link- und Pfadregeln, **24 Fehlerklassen** mit Präfix `KB04-` sowie zwei
> **reservierte, nicht implementierte** Exitcodes. **ADR-0014 bleibt unverändert die
> bindende A1-Authority**; `ADR_NOT_REQUIRED` gilt nur, solange der Vertrag
> vollständig innerhalb ADR-0014 bleibt. **B2 ist nicht autorisiert**, die
> ausführende Reparatur bleibt an **RT-2** gebunden und gesperrt. **KB-04 bleibt
> `DOCUMENTED ONLY`**, beide Gates `NOT EVALUATED`, Capabilities **0 von 29**, NT-04
> und NT-05 **nicht ausgeführt**, **SB-S04 nicht wirksam**, **R-20 offen**, **OD-37
> offen**, **R-33 18/21**.

> **D-059 nimmt eine Architektur an, sie autorisiert keine Umsetzung.** **D-059 trägt
> A0** (Human-Maintainer-Beschluss), **ADR-0014 trägt nach Annahme A1** — das
> bestehende Autoritätsmodell bleibt unverändert. Gewählt ist **Host-authoritative
> Enforcement mit deklarativem Zielmodell und read-only Runtime-Validierung**: das
> Repository hält nur das **abstrakte** Zielmodell, das Deployment setzt Besitz und
> Rechte **vor** dem Start, die Runtime **prüft ausschließlich und scheitert
> fail-closed**. **Keine Runtime-Komponente verändert jemals Besitz, Gruppe, Modus
> oder Identität.** Vierzehn Implementierungsparameter bleiben bewusst offen und
> gehören nach **B1C**; **B1C und B2 sind nicht autorisiert**. **KB-04 bleibt
> `DOCUMENTED ONLY`**, beide Gates `NOT EVALUATED`, Capabilities **0 von 29**, NT-04 und
> NT-05 **nicht ausgeführt**, **R-20 offen**, **OD-37 offen**, **R-33 18/21**.

> **D-058 stellt eine ADR-Pflicht fest, sie trifft keine Architekturwahl.** Die
> Entscheidung hält ausschließlich fest, **dass** vor einer technischen Umsetzung
> von KB-04 Enforcement Stage 1 ein **neuer ADR** erforderlich ist — voraussichtlich
> **ADR-0014**, das **nicht angelegt** wurde. Sie wählt **kein** Owner- oder
> Gruppenmodell, **keine** UID-/GID-Abbildung, **keine** Dateimodi, **keinen**
> Durchsetzungsakteur und **keine** Verifikations- oder Korrektursemantik. **B1B und
> B2 sind nicht autorisiert.** KB-04 bleibt `DOCUMENTED ONLY`, beide Runtime-Gates
> `NOT EVALUATED`, Capabilities **0 von 29**, NT-04 und NT-05 **nicht ausgeführt**,
> **R-20 offen**, **OD-37 offen**, **R-33 18/21**.

> **D-057 registriert ein Work Package, keine Umsetzung.** Die Entscheidung legt
> ausschließlich fest, **dass** CBP-WP-022 als Work Package geführt wird und
> **welche** bestehende Authority für KB-04 gilt. Sie autorisiert **keine**
> technische Implementierung, wählt **keine** Architektur und stuft **keine**
> Control hoch. **`ADR_NOT_REQUIRED` gilt nur für diese Registration-Decision**;
> die ADR-Frage der späteren Umsetzung ist vor einer B1-Freigabe **neu zu
> bewerten**. Zwölf Controls bleiben `DOCUMENTED ONLY`, beide Runtime-Gates
> `NOT EVALUATED`, Capabilities **0 von 29**, **R-20 offen**, **R-33 18/21**.

> **D-056 stellt eine Kennzahl fest, sie korrigiert sie nicht.** Die Entscheidung
> legt ausschließlich den **kanonischen Wert** des Security-Foundation-Test-
> inventars verbindlich fest und grenzt den Reconciliation-Scope ein. Sie ändert
> in Phase B0 **keine** Fundstelle, **kein** Bundle, **keinen** Validator und
> **keinen** Test. Bis zur Durchführung in B1/B2 besteht eine **bekannte,
> eingegrenzte Übergangsabweichung** zwischen dem festgestellten Wert **32** und
> den noch nicht korrigierten Artefakten. **Kein Test wurde ausgeführt**: 0 von
> 32 Negativtests, 0 von 1 Positivtest. Alle zwölf Security Controls bleiben
> `DOCUMENTED ONLY`, beide Runtime-Gates `NOT EVALUATED`, Capabilities **0 von
> 29**, **R-20 offen**.

> **D-055 autorisiert einen Pfad, kein Deployment.** Die Entscheidung legt
> ausschließlich fest, **wo** spätere Profil-A-Artefakte im Repository liegen
> dürfen. Sie erzeugt **kein** Verzeichnis, **keine** Artefakte, **keine**
> Bereitstellung und **keine** Migration. In Phase B0 wurde `deployments/`
> **nicht angelegt**. Alle zwölf Security Controls bleiben `DOCUMENTED ONLY`,
> beide Runtime-Gates `NOT EVALUATED`, Capabilities **0 von 29**, **R-20 offen**.

> **D-054 dokumentiert Zusagen, keine geprüfte Infrastruktur.** Alle Angaben zu
> Plattform, Ressourcen, Speicher, Netz und Sicherung sind **Aussagen des Human
> Maintainers**. Es wurde **kein** Hypervisor, **kein** Speicher, **kein**
> Netzwerk und **kein** Sicherungsziel berührt, geprüft oder verändert. **Keine
> Installation, kein Deployment, keine Betriebsfreigabe.** Der DRC steht auf
> **BLOCKED**; vier Prüfpunkte bleiben offen.

> **Die vier Entscheidungen legen Sicherheitsarchitektur fest, keine
> Implementierung.** Es wurde keine Identität angelegt, kein Recht gesetzt,
> kein Secret bereitgestellt, keine Netzwerkregel verändert und kein Test
> ausgeführt. Alle zwölf Kontrollen stehen auf **DOCUMENTED ONLY**, das
> [Security Foundation Readiness Gate](../docs/operations/SECURITY_FOUNDATION_READINESS_GATE.md)
> auf **`NOT EVALUATED`**.

> **D-038 bis D-041 (CBP-WP-013) definieren einen lokalen, synthetisch
> testbaren Quarantäneprototyp — keine produktive Quarantäne.** Es wurde keine
> reale Quelle berührt, kein Mapping aktiviert, nichts freigegeben und nichts
> promotet. **R-01 und R-32 bleiben offen**; **OD-05 und OD-06 bleiben offen**.
> Festgehalten in [ADR-0010](../docs/decisions/ADR-0010-ingest-quarantaene-mvp.md).

> **D-042 bis D-045 (CBP-WP-014) definieren einen lokalen, synthetisch
> testbaren, deaktivierten Source-Registry-Prototyp — keine produktive
> Registry, kein Mapping, keine Aktivierung.** Keine reale Quelle, kein Pfad,
> keine URL, kein Source-Inhalt berührt; jede Registrierung ist
> `REGISTERED_DISABLED`; `activate` verweigert immer. **Kein Risiko
> geschlossen**; **OD-05, OD-06, OD-37, OD-38 bleiben offen**. Festgehalten in
> [ADR-0011](../docs/decisions/ADR-0011-deterministische-source-registry.md).

> **D-046 bis D-049 (CBP-WP-015) definieren einen lokalen, synthetisch
> testbaren, read-only Validator für Mapping-Entwürfe — kein gespeichertes
> Mapping, keine Aktivierung, keine Vertragsänderung.** Der angenommene Vertrag
> bleibt bei **31 Felddefinitionen** (29 Pflicht + 2 optional). Keine reale
> Quelle, kein Pfad, keine URL, kein Source-Inhalt berührt; die Registry bleibt
> bytegenau unverändert; `activation-check` verweigert immer. **Kein Risiko
> geschlossen**; **OD-05, OD-06, OD-37, OD-38 bleiben offen**; die
> Bildungsvorschrift von `mapping_id` bleibt offen. Festgehalten in
> [ADR-0012](../docs/decisions/ADR-0012-source-mapping-draft-validator.md).

> **Hinweis zu D-016.** In CBP-WP-002 hatte ich „bevorzugte Anwendungslaufzeit"
> zu „vorgesehene" abgeschwächt (Ü-02), gestützt auf Projektübergabe §4 (A5),
> wonach Containerisierung kein Pflichtziel der ersten Phase ist. Der Human
> Maintainer hat nun ausdrücklich „bevorzugte Anwendungslaufzeit" bestätigt.
> **A0 schlägt A5** — die Abschwächung ist damit aufgehoben.
>
> **Nachgeführt in CBP-WP-004:** `docs/architecture/PROJECT_DEFINITION.md`
> trägt jetzt die korrigierte Formulierung mit ausdrücklicher Abgrenzung
> („bevorzugt, aber nicht einzige Laufzeit und keine Produktgrenze"). OD-31 ist
> damit geschlossen; festgehalten in **ADR-0002**.

## Geschlossene offene Entscheidungen

| ID | War offen | Geschlossen durch | Datum |
| --- | --- | --- | --- |
| OD-12 | Prompt Mode „Lean" | D-009 | 2026-07-20 |
| OD-02 | Definition der Context Budgets | CONTEXT_BUDGETS.md; Restpunkt OD-02b | 2026-07-20 |
| OD-01 | Kriterien für Gate G0 | G0_SCOPE_LOCK_CRITERIA.md, dreistufig klassifiziert | 2026-07-20 |
| **OD-21** | Zugriffsweg für Mehrgeräte-Nutzung | **D-023** — privates VPN/Netz; Technologie bleibt Deployment Required | 2026-07-20 |
| **OD-27** | Obsidian-Synchronisationsmodell | **D-025** — serverzentriert zuerst, native Nutzung vertagt | 2026-07-20 |
| **OD-10** | Verfahren bei Secret in der Git-Historie | [SECRET_INCIDENT_RESPONSE.md](../docs/security/SECRET_INCIDENT_RESPONSE.md) — 14 Schritte, Rotation vor Cleanup. **Technische Unterstützung bleibt offen** | 2026-07-20 |
| **OD-32** | Berechtigungsstufen je Bereich und Freigabeverfahren | [PERMISSION_MODEL.md](../docs/security/PERMISSION_MODEL.md) und ADR-0004 — 9 Rollen × 12 Ressourcen. **Technische Durchsetzung bleibt offen** | 2026-07-20 |
| **OD-31** | Nachführungen außerhalb des CBP-WP-003-Scopes | CBP-WP-004: D-016 in `PROJECT_DEFINITION.md` korrigiert, Kriterienzahl auf 47 aktualisiert, Korrektur-Notiz in `CBP-WP-002.md` ergänzt | 2026-07-20 |
| **OD-34** | Secret-Store-Technologie und Credential-Reference-Format | **D-035** — versionierter, providerneutraler Referenzvertrag `cbp-secret:v1:<provider>:<opaque-id>` plus OS-geschützter Datei-Provider für den Pilot; festgehalten in [SECRET_REFERENCE_AND_PROVIDER_CONTRACT.md](../docs/security/SECRET_REFERENCE_AND_PROVIDER_CONTRACT.md). **Konkreter Ablageort, Dateimodi und registrierte Providernamen bleiben Deployment Required** | 2026-07-21 |
| **OD-35** | RT-2: Aufbewahrung, Integritätsschutz, Backup-/Restore-Nachweis | **D-037** — logisch append-only, verkettet, aufbewahrungs- und sicherungspflichtig; festgehalten in [OPERATIONAL_EVIDENCE_POLICY.md](../docs/operations/OPERATIONAL_EVIDENCE_POLICY.md). **Die konkrete Aufbewahrungsdauer bleibt Deployment Required** und wird im DRC geprüft | 2026-07-21 |
| **OD-26** | Endgültige Repository-Struktur | **D-029 und D-030** — Ziel-Monorepo nach Layout-Option B **und** Bereichsmodell W-3; festgehalten in [ADR-0007](../docs/decisions/ADR-0007-repository-und-workspace-grenze.md). **Keine Reorganisation autorisiert**; AB-03…AB-08 bleiben offen (OD-29) | 2026-07-21 |
| **OD-33** | Definition des Deployment-Readiness-Gates | [DEPLOYMENT_READINESS_CHECK.md](../docs/operations/DEPLOYMENT_READINESS_CHECK.md) und ADR-0005 — DRC vollständig dokumentiert und in G0, Manifest, Profil, README und Brain verlinkt | 2026-07-20 |

## Vertagte Entscheidungen

Nicht geschlossen, aber für den Pilotumfang **nicht mehr blockierend**:

| ID | Entscheidung | Grund der Vertagung |
| --- | --- | --- |
| OD-09 | Rechtsgrundlage für personenbezogene Daten | D-022 — keine PII im Pilot; Prüfung vor späterer Aufnahme |
| OD-20 | Programmiersprache, Suchmaschine, Embedding-Modell | Deployment Required |
| OD-22 | Sicherungsfrequenz und Sicherungsziel | Deployment Required — **beantwortet in CBP-WP-019 (D-054)**: tägliche Sicherung, physisch separate NAS |
| OD-30 | Backup- und Restore-Zielwerte | Deployment Required — **beantwortet in CBP-WP-019 (D-054)**: RPO 24 h, RTO 8 h, DRC-16 `ready` im Umfang des Betreiber-Backup-Regimes |

Vertagt heißt **nicht erledigt**. Diese Punkte gehören in das
Deployment-Readiness-Gate.

## Offene Entscheidungen

Legende: **P0** blockiert G0 · **P1** vor Architekturentscheidung · **P2** später.

| ID | Offene Entscheidung | Prio | Adressat | Bezug |
| --- | --- | --- | --- | --- |
| OD-03 | Rang der Kernprinzipien: weitere ADRs über ADR-0001 bis ADR-0005 hinaus | P1 | Human Maintainer | teilweise durch ADR-0001…0005 |
| OD-04 | Minimal nützlicher Funktionsumfang des Piloten | **P0** | Nova | A-8 |
| OD-05 | Ablageort des kanonischen Wissensbestands — **präzisiert:** Slot-Art entschieden (PS-02, `operator-managed`), konkreter Ort offen. **Vorgesehen für CBP-WP-010** | P1 | Human Maintainer | D-1 `accepted`; PILOT_SOURCE_MAPPING_PLAN |
| OD-06 | Quellen im ersten Scope — **präzisiert:** Quellen*arten* und Slot-Regeln entschieden, konkreter Bestand offen. **Vorgesehen für CBP-WP-010** | P1 | Human Maintainer | D-1 `accepted`; PILOT_SOURCE_MAPPING_PLAN |
| OD-07 | Vergabeverfahren für Autoritätsklassen A0–A6 | **P0** | Nova | — |
| OD-08 | Vergabeverfahren für Datenklassen | **P0** | Human Maintainer | D-4 |
| OD-11 | Repository dauerhaft privat? | **P0** | Human Maintainer | A-8 |
| OD-29 | Dauerhafte Behandlung der NDF-Abweichungen AB-03 bis AB-08 | **P0** | Nova + Human Maintainer | ADOPTION_NOTES |
| OD-02b | Kalibrierung der Token-Richtwerte für B0–B4 | P1 | Nova | CONTEXT_BUDGETS |
| OD-13 | Manifest auf `project-manifest.yaml` umstellen | P1 | Nova | AB-03 |
| OD-14 | NDF-Namensschema für Register | P1 | Nova | AB-04 |
| OD-15 | Schnitt einer Wissenseinheit | P1 | Nova | — |
| OD-16 | Bildungsvorschrift der stabilen Source-ID | P1 | Nova | — |
| OD-17 | Verpflichtende Frontmatter-Felder | P1 | Nova | — |
| OD-18 | Filterreihenfolge im Retrieval-Pfad | P1 | Nova | TB-4 |
| OD-19 | Umfang und Format des Retrieval-Trace | P1 | Nova | — |
| OD-23 | Lizenzwahl | P1 | Human Maintainer | D-007 |
| OD-25 | qmd als produktiver Suchdienst — nur nach Prüfung | P1 | Human Maintainer | — |
| **OD-36** | Bildungsvorschrift der `mapping_id`, zulässige Collection-Namen und deren Vergabe, unterstützte `schema_version`-Werte über `1.0` hinaus | P1 | Nova | ADR-0008; PILOT_SOURCE_MAPPING_SCHEMA |
| **OD-37** | Produktive Quarantäne-Isolation auf der Ziel-VM (KB-03, KB-04): OS-Rechte, getrennte Identität, Unzugänglichkeit für den Indexer | P1 | Human Maintainer | ADR-0010; **Deployment Required** |
| **OD-38** | Produktive Secret- und PII-Erkennung: Werkzeugauswahl, Erkennungsgüte, Verfahren nach D-019 | P1 | Nova + Human Maintainer | ADR-0010; R-01, R-32 |
| OD-24 | Akzeptable Ausfallzeit | P2 | Human Maintainer | — |
| OD-28 | Öffentlicher Produktname und Phase-7-Option | P2 | Human Maintainer | — |

## Zusammenfassung

| Kategorie | Anzahl |
| --- | --- |
| Getroffene Entscheidungen | **61** (davon **57** mit A0) |
| Angenommene ADRs | **14** (ADR-0001 bis ADR-0014, alle A1) |
| Vorgeschlagene ADRs | 0 |
| Neu in CBP-WP-003 | 12 (D-015 bis D-026) |
| Neu in CBP-WP-004 | 0 Entscheidungen, 5 ADRs |
| Neu in CBP-WP-007 | **2 A0-Entscheidungen** (D-027 G0, D-028 ADR-0006) |
| Neu in CBP-WP-008 | 0 Entscheidungen, 1 neue offene Entscheidung (OD-34) |
| Neu in CBP-WP-009 | **2 A0-Entscheidungen** (D-029 Teil A, D-030 Teil B), **1 ADR** |
| Neu in CBP-WP-010 | **3 A0-Entscheidungen** (D-031 Format, D-032 Collection, D-033 Granularität), **1 ADR**, 2 neue offene Entscheidungen (OD-35, OD-36) |
| Neu in CBP-WP-011 | **4 A0-Entscheidungen** (D-034 Identity, D-035 Secret, D-036 Egress, D-037 Evidence), **1 ADR**, **2 geschlossene** (OD-34, OD-35) |
| Neu in CBP-WP-013 | **4 A0-Entscheidungen** (D-038 Intake, D-039 Store, D-040 Scanner, D-041 Freigabe), **1 ADR**, 2 neue offene Entscheidungen (OD-37, OD-38) |
| Neu in CBP-WP-014 | **4 A0-Entscheidungen** (D-042 Identität, D-043 Store, D-044 Lifecycle, D-045 Katalog), **1 ADR**, 0 neue offene Entscheidungen |
| Neu in CBP-WP-015 | **4 A0-Entscheidungen** (D-046 Dokumentprofil, D-047 Registry-Bindung, D-048 Boundary, D-049 Report/Aktivierung), **1 ADR** (ADR-0012), 0 neue offene Entscheidungen |
| Neu in CBP-WP-016 | **1 konsolidierte A0-Entscheidung** (D-050 Gate-Evaluator: A1/B1-eng/C1/D1), **0 ADR**, 0 neue offene Entscheidungen |
| Neu in CBP-WP-017 | **1 konsolidierte A0-Entscheidung** (D-051 Evidence-Contract: A2/B1/C2/D1/E2), **0 ADR**, 0 neue offene Entscheidungen |
| Neu in CBP-WP-018 | **2 konsolidierte A0-Entscheidungen** (D-052 Governance Foundation: A1/B1/C1/D1/E1 · D-053 Technical Implementation: A1/B1/C1/D1/E1), **1 ADR** (ADR-0013 Evidence Schema 3.0), D-051 partiell abgelöst (C2/D1), 0 neue offene Entscheidungen |
| Neu in CBP-WP-019 | **1 konsolidierte A0-Entscheidung** (D-054 Deployment Readiness Intake: A/B/C/D/E/F/G/H), **0 ADR** (`ADR_NOT_REQUIRED`), 0 neue offene Entscheidungen; **OD-22 und OD-30 beantwortet**, DRC von 18 auf **19** Prüfpunkte erweitert |
| Neu in CBP-WP-020 | **1 konsolidierte A0-Entscheidung** (D-055 Additive Deployment-Artefaktstruktur und WP-020-Scope: A–J), **0 ADR** (`ADR_NOT_REQUIRED`), 0 neue offene Entscheidungen, **keine neue Risiko-ID**; `deployments/profile-a/` als späterer B1-Pfad autorisiert, **nicht angelegt** |
| Neu in CBP-WP-021 | **1 konsolidierte A0-Entscheidung** (D-056 Kanonisches Security-Testinventar: A–U), **0 ADR** (`ADR_NOT_REQUIRED`), 0 neue offene Entscheidungen, **keine neue Risiko-ID**; kanonisch **32 Negativtests + 1 Positivtest = 33 Testfälle**, **0 ausgeführt**; Durchführung der Reconciliation erst in B1/B2 |
| Neu in CBP-WP-022 | **5 konsolidierte A0-Entscheidungen** — **D-057** (Registrierung, A–M; `ADR_NOT_REQUIRED` **nur für die Registration**), **D-058** (ADR-Gate, A–M; Ergebnis **`ADR_REQUIRED`**), **D-059** (Architekturentscheidung, A–N; Ergebnis **`ADR-0014_ACCEPTED`**), **D-060** (Enforcement Contract, A–S; Ergebnis **`KB-04_STAGE_1_CONTRACT_ACCEPTED`**) und **D-061** (B2C-Scope, A–R; Ergebnis **`B2C_TRACEABILITY_AND_NT_PREPARATION_SELECTED`**) — die letzten drei je **`ADR_NOT_REQUIRED`** innerhalb ADR-0014; **1 ADR** — **ADR-0014** *KB-04 Stage 1 Filesystem Enforcement Architecture*, `accepted`, **A1**; 0 neue offene Entscheidungen, **keine neue Risiko-ID**; **keine Implementierungsfreigabe** — B2C-T, B2B-Apply und B2D nicht autorisiert |
| Geschlossene offene Entscheidungen | **12** |
| Vertagte Entscheidungen | 4 |
| Offene Entscheidungen | **23** |
| davon **P0** | **5** — OD-04, OD-07, OD-08, OD-11, OD-29 |

*Sämtliche Werte in CBP-WP-011 aus den Quelltabellen **ausgezählt**, nicht
fortgeschrieben (Zählregel 1 und 2). Jede gezählte Einheit ist einzeln
benennbar — kein offener Zusatz.*

> **Zu OD-35:** Die drei RT-2-Punkte waren inhaltlich bereits in ADR-0007 als
> „offene Folgefragen" benannt, aber in keinem Register geführt. Dieser Eintrag
> ist **keine Dublette**, sondern macht sie nachverfolgbar. Sie werden als
> **ein** Punkt geführt, nicht als drei.

> **Korrektur in CBP-WP-008 — vierter Zählfehler des Projekts.** Die
> Summenzeile führte zuvor **22 A0-Entscheidungen** und **8 offene
> P0-Entscheidungen**. Die Auszählung der Tabellen ergibt **24** und **6**. Die
> Tabellen selbst waren korrekt; nur die Summen stimmten nicht.
>
> Die sechs offenen P0-Punkte sind OD-04, OD-07, OD-08, OD-11, OD-26 und OD-29.
>
> **Der Fehler bestand seit CBP-WP-007 und wurde durch die Zähl- und
> Statusregel gefunden — aber erst ein Work Package später.** Genau deshalb
> bleibt **R-33 offen**: die Regel macht Fehler später sichtbar, sie verhindert
> sie nicht.

**Keine offene Entscheidung wird als A0 geführt.** Keine Entscheidung wurde aus
reinen Sachangaben abgeleitet — die Infrastrukturangaben aus dem Intake sind
Human-Evidenz, keine Beschlüsse.

## Pflege

Eine getroffene Entscheidung wird **nicht gelöscht**. Änderungen erfolgen durch
einen neuen Eintrag, der den alten als ersetzt kennzeichnet.
